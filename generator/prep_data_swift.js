// SWIFT — 20 тем, розширена версія
module.exports = {
  title: "SWIFT — 21 тема",
  note: "Мова — це 50–60% технічної співбесіди. Особливо памʼять і конкурентність: саме тут відсіюють між junior і middle.",
  topics: [
    {
      p: "P0", name: "Value vs Reference types, struct vs class",
      also: ["swift-14", "swift-16"],
      what: [
        "Value-типи (struct, enum, tuple) при присвоєнні та передачі у функцію копіюються — кожна змінна володіє власним незалежним значенням. Reference-типи (class, closure, actor) передаються за посиланням: кілька змінних вказують на один обʼєкт у купі (heap), і зміна через одне посилання видна всім.",
        "Стандартні колекції (Array, String, Dictionary, Set) — value-типи з оптимізацією Copy-on-Write: фізичне копіювання буфера відбувається лише в момент першої мутації, а не при присвоєнні. Тому передати масив на 100 000 елементів у функцію — дешево, доки функція його не змінює.",
        "Identity (тожність) існує лише у reference-типів: === питає «це той самий обʼєкт у памʼяті?», == — «значення рівні?» (Equatable). Для struct === немає взагалі: у двох копій нема спільної адреси, яку можна порівнювати. Із identity звʼязаний і ARC — лічильник посилань має сенс тільки там, де є посилання, тому retain/release, weak та unowned — історія виключно про class (і closure/actor).",
      ],
      problem: "Головна проблема, яку вирішують value-типи — спільний мутабельний стан (shared mutable state): коли два шматки коду тримають один обʼєкт і несподівано змінюють його один одному «під ногами». З value-типами про шматок коду можна міркувати локально: ніхто ззовні твою копію не змінить. Це також безкоштовна потокобезпечність — копії незалежні.",
      where: [
        "Моделі даних і DTO з бекенда — майже завжди struct (у твоєму проєкті LocationAddress, моделі замовлення тощо). Codable + struct — стандартна пара.",
        "SwiftUI: кожна View — struct. Уся ідеологія фреймворку побудована на дешевих value-«описах» інтерфейсу.",
        "class — коли потрібна ідентичність та спільний стан: мережевий менеджер, кеш, сесія користувача, ViewModel (ObservableObject), усе успадковане від UIKit (UIViewController, UIView — класи, бо їм потрібна саме ідентичність конкретного обʼєкта на екрані).",
        "enum — для станів і скінченних варіантів: стан екрана, тип оплати, маршрути дип-лінків.",
      ],
      code: [
        "struct Point { var x = 0 }",
        "var a = Point(); var b = a",
        "b.x = 10                    // a.x == 0 — незалежна копія",
        "",
        "final class Counter { var value = 0 }",
        "let c1 = Counter(); let c2 = c1",
        "c2.value = 10               // c1.value == 10 — один обʼєкт",
        "// let для класу фіксує ПОСИЛАННЯ, не вміст!",
        "",
        "let c3 = Counter()",
        "c1 === c2                   // true — те саме посилання (identity)",
        "c1 === c3                   // false — інший обʼєкт",
        "// для struct === не існує: порівнювати нема чого",
      ],
      traps: [
        "Struct, який містить class-властивість: копіюється struct, але посилання всередині вказує на той самий обʼєкт — «value-семантика» ламається.",
        "let класу не робить обʼєкт незмінним — лише забороняє переприсвоїти посилання. let структури — заморожує все.",
        "mutating-метод структури неможливо викликати на let-змінній — і це фіча, а не обмеження.",
        "Дуже великий struct, який часто копіюється у гарячому коді, може програти класу за перформансом — міряй Instruments, а не вгадуй.",
      ],
      qs: [
        {
          q: "Що виведе код, де struct присвоєно у дві змінні й одну змінили? А якщо це class?",
          a: [
            "Зі struct друга змінна отримує незалежну копію: змінив b.x — a.x лишився старим. З class обидві змінні тримають посилання на той самий обʼєкт у купі, тому зміна через c2 видна і через c1.",
            "Це вся різниця у двох реченнях: value-тип копіює значення, reference-тип копіює посилання.",
          ],
        },
        {
          q: "Чому Apple рекомендує struct за замовчуванням — і в яких випадках ти одразу обереш class?",
          a: [
            "Про struct легше міркувати: у тебе власна копія, ніхто ззовні її не змінить, немає ARC-накладних і немає ризику retain cycle.",
            "Class беру, коли потрібна ідентичність або спільний стан: мережевий менеджер, кеш, сесія, ViewModel (ObservableObject), усе успадковане від UIKit — і коли обʼєкту потрібен deinit.",
          ],
        },
        {
          q: "Як працює Copy-on-Write і як перевірити, що фізична копія буфера дійсно відбулася?",
          a: [
            "Колекція тримає дані у reference-буфері. При присвоєнні копіюється лише посилання, а перед першою мутацією тип перевіряє isKnownUniquelyReferenced: якщо власник не один — щойно тоді буфер дублюється.",
            "Перевірити можна адресою: узяти адресу першого елемента до і після мутації — змінилась, значить копія відбулась.",
          ],
          code: [
            "var a = [1, 2, 3]",
            "var b = a                       // буфер спільний",
            "a.withUnsafeBufferPointer { print($0.baseAddress!) }",
            "b.withUnsafeBufferPointer { print($0.baseAddress!) }   // та сама адреса",
            "b[0] = 99                       // мутація → копія",
            "b.withUnsafeBufferPointer { print($0.baseAddress!) }   // інша адреса",
          ],
        },
        {
          q: "Як COW впливає на продуктивність передачі великого масиву у функцію? Чому Array — struct, але копіювання великого масиву майже нічого не коштує?",
          a: [
            "Передача — O(1): копіюється посилання на буфер плюс retain. Ціна зʼявляється лише якщо функція масив змінює — тоді буде одна фізична копія O(n).",
            "Тому «Array — value-тип» не означає «дорогий»: семантика значення тут реалізована через посилання під капотом.",
          ],
        },
        {
          q: "Чим відрізняються == і ===? Що таке identity і чому вона є лише у class?",
          a: [
            "== порівнює значення і вимагає Equatable; === порівнює, чи це той самий обʼєкт у памʼяті, тобто identity.",
            "Identity є лише у reference-типів, бо тільки там кілька змінних діляться однією адресою. Для двох копій struct питання «той самий?» не має сенсу — === для них навіть не компілюється.",
          ],
        },
        {
          q: "Що таке shared mutable state і чому він небезпечний?",
          a: [
            "Це коли кілька частин коду тримають один мутабельний обʼєкт: хтось його змінює, а решта цього не очікує. Наслідок — баги «я цього не робив», а в багатопотоковому коді ще й data race.",
            "Value-типи прибирають проблему за визначенням — у кожного своя копія. Там, де спільний стан справді потрібен, його загороджують: serial черга, барʼєр або actor.",
          ],
        },
        {
          q: "Чому let для class не робить обʼєкт immutable, а let для struct забороняє змінювати будь-яку властивість?",
          a: [
            "let фіксує вміст самої змінної. У класу вміст — це посилання: інший обʼєкт присвоїти не можна, а змінювати властивості всередині — можна.",
            "У структури вміст — це саме значення з усіма полями, тому let заморожує все і забороняє навіть mutating-методи.",
          ],
        },
        {
          q: "Що означає ключове слово mutating і чому воно потрібне саме структурам?",
          a: [
            "mutating дозволяє методу структури змінювати self: фактично метод отримує self як inout і присвоює на місце нове значення.",
            "Класам це не потрібно — метод змінює обʼєкт за посиланням, сам self лишається тим самим. Саме тому mutating-метод неможливо викликати на let-структурі.",
          ],
        },
        {
          q: "Чому ARC працює лише для class? Що таке reference counting і для чого потрібні weak та unowned?",
          a: [
            "ARC рахує сильні посилання на обʼєкт у купі; коли лічильник падає до нуля — deinit і звільнення. Виклики retain/release вставляє компілятор на етапі збірки.",
            "У value-типів немає спільного обʼєкта, на який посилаються, тому рахувати нічого. weak і unowned не збільшують лічильник: перше обнуляється після смерті обʼєкта, друге ні (звертання = краш). Потрібні, щоб розривати retain cycle.",
          ],
        },
        {
          q: "Що станеться, якщо struct містить властивість типу class?",
          a: [
            "Скопіюється struct, але посилання всередині вкажуть на той самий обʼєкт — value-семантика ламається: зміна через копію видна в оригіналі.",
            "Потрібна справжня незалежність — роби глибоку копію вручну або тримай усередині value-тип.",
          ],
        },
        {
          q: "Чому UIViewController — це class, а View у SwiftUI — struct?",
          a: [
            "Контролер — живий обʼєкт із власним життєвим циклом та ідентичністю: система тримає на нього посилання, надсилає колбеки, він успадкований від UIKit-класу і має deinit.",
            "SwiftUI-View — не обʼєкт на екрані, а дешевий опис того, як екран має виглядати. Фреймворк створює його заново на кожен перерендер, тому value-тип тут природний.",
          ],
        },
        {
          q: "Чому ObservableObject майже завжди реалізують як class, а моделі (Codable) — як struct?",
          a: [
            "ObservableObject мусить бути посиланням: кілька View підписуються на одне джерело стану і мають бачити ті самі зміни — тому протокол і обмежений AnyObject.",
            "Моделі з бекенда навпаки передають, копіюють і порівнюють: тут потрібні value-семантика, memberwise init і безкоштовна потокобезпечність — struct + Codable.",
          ],
        },
        {
          q: "Коли великий struct може працювати повільніше за class? Чим відрізняється передача struct і class у функцію?",
          a: [
            "Class передається як одне посилання, скільки б полів не мав. Struct передається копіюванням усіх полів — якщо він великий і копіюється в гарячому циклі, це вже помітно.",
            "Але це саме той випадок, коли правильна відповідь —  «поміряю в Instruments»: компілятор часто передає struct за посиланням неявно, а COW прибирає найбільшу частину витрат.",
          ],
        },
        {
          q: "Де саме в твоєму проєкті ти використовував struct, а де — class, і чому?",
          a: [
            "Моделі й DTO — struct + Codable (LocationAddress, моделі замовлення), стани екрана — enum; мережевий шар, кеш, сесія і ViewModel — class, бо це спільний стан з ідентичністю.",
            "Тут перевіряють не теорію, а чи ти справді ухвалював це рішення — називай конкретний тип зі свого коду.",
          ],
        },
        {
          q: "Наведи приклад, коли value-семантика врятувала від бага, і приклад, коли потрібна була саме reference-семантика.",
          a: [
            "Value: екран редагування отримує копію моделі, юзер щось міняє і тисне «Скасувати» — оригінал у списку цілий, бо його ніхто не торкався. З класом довелося б копіювати руками.",
            "Reference: кошик або сесія користувача — один обʼєкт, який усі екрани мусять бачити однаково; копії тут якраз і були б багом.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Optionals",
      what: [
        "Optional<T> — це звичайний enum з двома кейсами: .some(T) і .none. Синтаксис T? — лише цукор. Optional вбудовує «відсутність значення» у систему типів: компілятор змушує явно обробити кейс nil ще до запуску.",
        "Способи роботи: if let / guard let (безпечне розгортання), оператор ?? (значення за замовчуванням), optional chaining user?.address?.city (ланцюг обривається на першому nil), map/flatMap на Optional, force unwrap ! (крешить на nil).",
      ],
      problem: "Вирішує проблему «мільярдної помилки» — null pointer. В Objective-C виклик методу на nil мовчки нічого не робив (приховані баги), у Java NPE валив програму в рантаймі. Swift переносить цю помилку на етап компіляції: тип String і String? — різні типи, і забути перевірку неможливо.",
      where: [
        "Відповіді API: поля, яких може не бути у JSON — var promoCode: String? у моделі замовлення. Codable сам ставить nil для відсутніх ключів, якщо поле опціональне.",
        "UI-стан «ще не завантажено»: var user: User? = nil до приходу відповіді.",
        "Повернення з пошуку: dictionary[key], array.first, Int(\"abc\") — усе повертає Optional, бо результату може не бути.",
        "guard let — стандарт на початку методу: перевірили вхідні дані й далі працюємо з не-опціоналами (early exit).",
      ],
      code: [
        "func handle(response: Data?) {",
        "    guard let data = response else {",
        "        showError(); return   // early exit — далі data гарантовано є",
        "    }",
        "    let order = try? decoder.decode(Order.self, from: data)",
        "    // try? повертає Order? — помилка перетворилась на nil",
        "    label.text = order?.address?.city ?? \"—\"",
        "}",
      ],
      traps: [
        "Force unwrap (!) у продакшні — креш, який знайде користувач. Легітимні винятки: IBOutlet (система гарантує звʼязування), ресурси в бандлі, які точно існують.",
        "try? + функція, що повертає Optional = подвійний optional T?? — легко заплутатись.",
        "if let x = x всередині кожного if — «піраміда»; краще один guard з комою: guard let a, let b else.",
        "map vs flatMap на Optional: map загортає результат ще раз (може дати T??), flatMap сплющує.",
      ],
      qs: [
        {
          q: "Що таке Optional під капотом? Напиши його оголошення як enum.",
          a: [
            "Optional — generic enum із двох кейсів: none і some(Wrapped). String? — цукор для Optional<String>, а nil — це .none.",
            "Тому з ним працює вся механіка enum: switch по кейсах, pattern matching, map/flatMap.",
          ],
          code: [
            "enum Optional<Wrapped> {",
            "    case none",
            "    case some(Wrapped)",
            "}",
            "let s: String? = nil            // .none",
            "if case .some(let v) = s { print(v) }",
          ],
        },
        {
          q: "Чим map на Optional відрізняється від flatMap?",
          a: [
            "map застосовує функцію до значення, якщо воно є, і загортає результат в Optional. Якщо функція сама повертає Optional — отримаєш подвійну обгортку Int??.",
            "flatMap сплющує результат до одного рівня. Практично: map — для перетворень, що завжди вдаються, flatMap — коли перетворення саме може дати nil.",
          ],
          code: [
            "let a: String? = \"42\"",
            "a.map { Int($0) }        // Int?? — подвійна обгортка",
            "a.flatMap { Int($0) }    // Int?  — те, що треба",
          ],
        },
        {
          q: "Чому force unwrap — червоний прапорець і які винятки прийнятні?",
          a: [
            "! означає «я гарантую, що тут не nil», а ціна помилки — краш у користувача замість помилки компіляції. У коді, що працює з даними ззовні (мережа, JSON, URL із рядка), такої гарантії немає.",
            "Прийнятно: @IBOutlet, які створює система; ресурси з бандла, відсутність яких і є баг збірки; try!/! у тестах, де краш = червоний тест; і там, де guard уже перевірив вище.",
          ],
        },
        {
          q: "guard let vs if let — коли що і чому guard покращує читабельність?",
          a: [
            "guard let — для раннього виходу: розгорнуте значення живе далі у функції, а happy path не заїжджає вправо драбинкою вкладень.",
            "if let — коли обидві гілки рівноправні або треба щось зробити саме у випадку nil. Правило: передумови на початку функції — guard, локальне розгалуження — if let.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Properties: stored, computed, lazy, observers, wrappers",
      what: [
        "Stored — зберігає значення в памʼяті обʼєкта. Computed — не зберігає нічого, обчислюється при кожному звертанні (get, опційно set). lazy — stored, який ініціалізується при першому звертанні, а не при створенні обʼєкта. Observers willSet/didSet — хуки навколо запису у stored property.",
        "Property wrapper — тип із var wrappedValue, який інкапсулює логіку зберігання; @State, @Published, @AppStorage — усе це property wrappers. projectedValue (доступ через $) дає додаткове представлення — наприклад, Binding у SwiftUI.",
        "lazy vs computed — за кількістю обчислень і памʼяттю: lazy обчислюється ОДИН раз при першому звертанні і далі займає памʼять під збережене значення; computed памʼяті під значення не займає, але виконує тіло при КОЖНОМУ звертанні. Звідси правило: lazy — для дорогого й незмінного (formatter, важкий парсер), computed — для похідного, що має бути завжди актуальним.",
        "projectedValue — друге, «проєктоване» представлення обгортки: компілятор перетворює $x на x.projectedValue. У @Published це Publisher (можна .sink), у @State/@Binding — Binding<Value>, який передають у дочірню View, у власному wrapper — будь-що корисне. Не оголосив projectedValue — $ для цієї обгортки просто не працює.",
        "@State: до Xcode 27 — property wrapper, реалізований через механізм @propertyWrapper; з Xcode 27 / SwiftUI 2026 — macro: код керування станом генерується під час компіляції. API для розробника лишився таким самим, змінилась тільки реалізація під капотом.",
      ],
      problem: "lazy вирішує проблему дорогої ініціалізації, яка може не знадобитись (важкий DateFormatter, обчислення на великих даних). Observers — проблему «тримати щось синхронним зі значенням» без ручних викликів (оновити UI при зміні моделі). Wrappers — проблему копіпасти навколо зберігання (та сама логіка UserDefaults для 20 налаштувань — один wrapper замість 20 однакових get/set).",
      where: [
        "lazy var formatter: DateFormatter — класика: формуттери дорогі, створюємо один раз при першому використанні.",
        "didSet на властивості ViewModel/клітинки: var order: Order? { didSet { updateUI() } } — типовий патерн конфігурації UITableViewCell.",
        "@UserDefault(\"onboarding_shown\") var shown: Bool — свій wrapper для налаштувань (поширене питання «напишіть property wrapper»).",
        "Computed для похідних даних: var fullPrice: Decimal { items.reduce(0) { $0 + $1.price } } — завжди актуальне, немає ризику розсинхрону.",
      ],
      code: [
        "@propertyWrapper struct UserDefault<T> {",
        "    let key: String; let defaultValue: T",
        "    var wrappedValue: T {",
        "        get { UserDefaults.standard.object(forKey: key) as? T ?? defaultValue }",
        "        set { UserDefaults.standard.set(newValue, forKey: key) }",
        "    }",
        "}",
        "struct Settings {",
        "    @UserDefault(key: \"isDark\", defaultValue: false) static var isDark: Bool",
        "}",
        "",
        "// @Clamped(0...100) — обрізає значення і в init, і при кожному записі",
        "@propertyWrapper struct Clamped<T: Comparable> {",
        "    private var value: T",
        "    let range: ClosedRange<T>",
        "    init(wrappedValue: T, _ range: ClosedRange<T>) {   // wrappedValue — завжди перший",
        "        self.range = range",
        "        self.value = min(max(wrappedValue, range.lowerBound), range.upperBound)",
        "    }",
        "    var wrappedValue: T {",
        "        get { value }",
        "        set { value = min(max(newValue, range.lowerBound), range.upperBound) }",
        "    }",
        "    var projectedValue: ClosedRange<T> { range }        // $percent → 0...100",
        "}",
        "struct Upload { @Clamped(0...100) var percent: Int = 0 }",
        "var u = Upload(); u.percent = 150      // 100 — обрізало; u.$percent → 0...100",
      ],
      traps: [
        "lazy НЕ потокобезпечний: два потоки при першому звертанні можуть ініціалізувати двічі. І lazy — тільки var.",
        "willSet/didSet не викликаються під час init (пряме присвоєння у сховище) — частий сюрприз.",
        "Computed property обчислюється при КОЖНОМУ звертанні — дорогий обрахунок у ньому в циклі = просідання.",
        "didSet, який присвоює цю ж властивість — рекурсії не буде (спрацює лише раз), але код заплутаний.",
        "Параметр обгортки передається лише через init(wrappedValue:_:), і wrappedValue у ньому мусить бути ПЕРШИМ аргументом — інакше @Clamped(0...100) не скомпілюється.",
        "@State з Xcode 27 — macro, а не property wrapper: API той самий, але в поясненні «як це працює» вже не варто спиратися на @propertyWrapper-механіку.",
      ],
      qs: [
        {
          q: "Чим lazy var відрізняється від computed property за кількістю обчислень і памʼяттю?",
          a: [
            "lazy обчислюється один раз — при першому звертанні — і далі займає памʼять під збережене значення. Computed памʼяті під значення не займає, але виконує тіло щоразу.",
            "Звідси вибір: дороге й незмінне (DateFormatter, важкий парсер) — lazy; похідне, що має бути завжди актуальним (сума кошика) — computed. І lazy буває тільки var, бо змінює себе при першому доступі.",
          ],
        },
        {
          q: "Чому didSet не викликається з ініціалізатора і як усе-таки виконати логіку?",
          a: [
            "Під час init властивість не «змінюється», а вперше записується у сховище — обсерверів на цьому шляху немає. Це навмисно: обʼєкт ще не готовий, викликати з нього методи небезпечно.",
            "Якщо логіка потрібна і при створенні — виносиш її в окремий метод і кличеш явно після повної ініціалізації (у кінці init або вже у viewDidLoad).",
          ],
        },
        {
          q: "Напиши property wrapper @Clamped(0...100) для відсотків.",
          a: [
            "Ключове: обрізати треба і в init(wrappedValue:_:), і в set — інакше початкове значення пролізе за межі. Діапазон передається через init, у якому wrappedValue мусить бути першим аргументом.",
            "projectedValue тут логічно віддати як сам діапазон — тоді $percent покаже межі.",
          ],
          code: [
            "@propertyWrapper struct Clamped<T: Comparable> {",
            "    private var value: T",
            "    let range: ClosedRange<T>",
            "    init(wrappedValue: T, _ range: ClosedRange<T>) {",
            "        self.range = range",
            "        self.value = min(max(wrappedValue, range.lowerBound), range.upperBound)",
            "    }",
            "    var wrappedValue: T {",
            "        get { value }",
            "        set { value = min(max(newValue, range.lowerBound), range.upperBound) }",
            "    }",
            "    var projectedValue: ClosedRange<T> { range }",
            "}",
            "struct Upload { @Clamped(0...100) var percent: Int = 0 }",
          ],
        },
        {
          q: "Що таке projectedValue і що повертає $ у @Published/@State?",
          a: [
            "projectedValue — друге представлення обгортки: компілятор перетворює $x на x.projectedValue.",
            "У @Published це Publisher — на нього підписуються через sink. У @State і @Bindable це Binding<Value>, який передають дочірній View, щоб вона могла писати у стан батька. Не оголосив projectedValue — $ для твоєї обгортки просто не існує.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Closures і capture semantics",
      what: [
        "Closure — блок коду першого класу, який можна зберігати і передавати. Це reference-тип: він живе в купі й захоплює (capture) змінні з контексту створення. var захоплюється за посиланням — closure бачить актуальне значення і може його змінювати. Capture list ([x], [weak self]) — копіює значення/послаблює посилання В МОМЕНТ СТВОРЕННЯ closure.",
        "@escaping — closure переживає виклик функції (зберігається, викликається пізніше — completion handlers). Non-escaping (за замовчуванням) виконується до повернення функції, тому не вимагає self і не створює циклів.",
        "Retain cycle через closure потребує ДВОХ сильних посилань одночасно: self утримує closure (наприклад, тримає його у властивості) і closure сильно захоплює self. Не виконано хоч одну умову — циклу немає. Саме тому DispatchQueue.global().async { self.load() } безпечний: closure тимчасово тримає черга, self черги не тримає, після виконання все звільняється.",
        "Практичне правило перед тим, як писати closure: «хто буде його зберігати?». Ніхто (map, filter, sorted, разовий UIView.animate) — [weak self] зазвичай зайвий. Зберігає інший обʼєкт (властивість, completion handler, callback у ViewModel чи сервісі) — перевір, чи не захоплюється self сильно, і розривай через [weak self].",
      ],
      problem: "Closures вирішують проблему «передати поведінку як дані»: колбеки завершення асинхронних операцій, конфігурація (map/filter), відкладене виконання. Capture list вирішує дві проблеми: retain cycle (closure тримає self, а self тримає closure) і «захоплення останнього значення» у циклах.",
      where: [
        "Completion handlers мережевих запитів (до async/await, і досі в legacy): fetchOrders { [weak self] result in ... }.",
        "Колбеки UI: кнопка в клітинці повідомляє контролер через var onTap: (() -> Void)? — стандартний патерн у твоєму UIKit-коді.",
        "Функціональні операції: orders.filter { $0.isActive }.map(\\.id).",
        "DispatchQueue.main.async { }, анімації UIView.animate — усюди closures.",
      ],
      code: [
        "var handlers: [() -> Void] = []",
        "for i in 1...3 {",
        "    handlers.append { print(i) }          // 1 2 3 — for створює нову константу i",
        "}",
        "var x = 0",
        "let a = { print(x) }                      // захоплення за посиланням",
        "let b = { [x] in print(x) }               // копія на момент створення",
        "x = 99",
        "a() // 99        b() // 0",
        "",
        "final class ViewModel {",
        "    var completion: (() -> Void)?",
        "    func setup() {",
        "        completion = { self.loadData() }                  // ❌ VM → closure → VM",
        "        completion = { [weak self] in self?.loadData() }   // ✅ коло розірвано",
        "    }",
        "}",
      ],
      traps: [
        "Retain cycle: self.completion = { self.doSomething() } — self тримає closure, closure тримає self. Розрив: [weak self].",
        "[weak self] робить self опціональним усередині — guard let self else { return } на початку.",
        "unowned self — креш, якщо closure викличеться після смерті self. Тільки коли життя closure гарантовано коротше за self.",
        "[weak self] не потрібен там, де closure не зберігається надовго: разовий DispatchQueue.main.async лише відкладе звільнення на мить — це не цикл.",
        "Non-escaping closure не вимагає явного self і не може створити цикл — питання про [weak self] взагалі не стоїть, доки closure не @escaping.",
      ],
      qs: [
        {
          q: "Що виведе код вище і чому a() та b() дають різні результати?",
          a: [
            "a() виведе 99, b() — 0. Closure a захопила змінну x за посиланням, тому бачить актуальне значення на момент виклику.",
            "У b capture list [x] зробив копію значення в момент СТВОРЕННЯ closure — тоді x був 0, і зміни після цього на неї не впливають.",
          ],
        },
        {
          q: "Чому @escaping вимагає явного self? Що перевіряє компілятор?",
          a: [
            "Escaping closure переживе виклик функції, тому захоплення self — це рішення про час життя обʼєкта, а не деталь синтаксису. Явний self змушує тебе це рішення побачити.",
            "Компілятор гарантує, що non-escaping closure не переживе функцію (її можна тримати на стеку), а escaping переносить у купу і вимагає, щоб усе захоплене жило достатньо довго.",
          ],
        },
        {
          q: "Namaлюй retain cycle через closure і два способи розірвати.",
          a: [
            "Цикл: self тримає closure у властивості, а closure сильно захоплює self — лічильники обох ніколи не впадуть до нуля.",
            "Спосіб перший — [weak self] і self?. (або guard let self else { return }). Спосіб другий — не тримати closure зовсім: занулити її після виклику або передавати як non-escaping. Третій, з обережністю, — [unowned self], коли час життя гарантований.",
          ],
          code: [
            "self.completion = { self.reload() }                    // ❌ цикл",
            "self.completion = { [weak self] in self?.reload() }    // ✅ weak",
            "// або розірвати вручну: викликав → completion = nil",
          ],
        },
        {
          q: "Коли [weak self] зайвий і лише додає шуму?",
          a: [
            "Коли closure ніхто не зберігає: map/filter/sorted, UIView.animate, разовий DispatchQueue.main.async — вона виконається і звільниться, циклу немає. Так само коли self у closure узагалі не згадується.",
            "Зайвий weak шкодить не лише шумом: у разовому запиті колбек може тихо не виконатись, бо self уже nil, — і замість оновлення екрана ти отримаєш «нічого не сталося».",
          ],
        },
        {
          q: "Дві умови, за яких виникає retain cycle через closure — і чому DispatchQueue.global().async їх не виконує?",
          a: [
            "Потрібні обидві: (1) self утримує closure — напряму або через інший обʼєкт; (2) closure сильно захоплює self. Не виконана хоч одна — циклу немає.",
            "У global().async closure тримає черга, а не self; після виконання вона її відпускає. Тобто перша умова не виконується.",
          ],
        },
        {
          q: "Escaping vs non-escaping: коли виконується, чи зберігається, чи можливий цикл, чи потрібен явний self?",
          a: [
            "Non-escaping (за замовчуванням): виконується до повернення функції, не зберігається, цикл неможливий, self — без явного self.",
            "Escaping (@escaping): може виконатись пізніше, може зберігатися у властивості або піти в асинхронну чергу, цикл можливий, self — тільки явно.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "ARC, memory management, retain cycles",
      what: [
        "ARC (Automatic Reference Counting) — компілятор на етапі збірки вставляє retain/release виклики. Кожен обʼєкт має лічильник сильних посилань; коли він падає до нуля — deinit і звільнення. На відміну від garbage collection: детерміновано (обʼєкт помирає одразу), без пауз збирача, але не вміє розривати цикли — це відповідальність розробника.",
        "weak — не збільшує лічильник, автоматично стає nil після смерті обʼєкта (тому завжди optional var). unowned — не збільшує лічильник, але НЕ обнуляється: звертання після смерті = креш. strong — за замовчуванням.",
        "«Власник» — це той, хто має strong-посилання, і тим самим відповідає за час життя обʼєкта. Головне правило ARC формулюється саме через власність: strong тримає лише власник, усі інші посилання (delegate, зворотне посилання на батька, self у збереженому closure) — weak або, рідше, unowned. За замовчуванням strong усюди: властивості, елементи Array/Dictionary/Set, локальні змінні та параметри функцій.",
        "weak можна повісити тільки на властивість класу — weak для локальної змінної чи параметра функції не компілюється. unowned не є Optional і не обнуляється: звертання після смерті обʼєкта = краш, тому його беруть лише там, де час життя власника гарантовано довший (класика — CreditCard, що не існує без Customer).",
      ],
      problem: "Вирішує проблему ручного керування памʼяттю (retain/release руками в старому Obj-C — джерело крешів і ліків). Retain cycle — головна ціна: два обʼєкти тримають один одного сильно, лічильники ніколи не впадуть до нуля, память тече. У великому застосунку типовий симптом — контролер не деінітиться і продовжує слухати нотифікації/оновлювати дані.",
      where: [
        "delegate завжди weak var delegate: SomeDelegate? — інакше цикл: VC тримає tableView, tableView тримає delegate (VC).",
        "closure + self — див. тему closures; це 90% реальних ліків.",
        "Timer.scheduledTimer(target: self) — RunLoop тримає таймер, таймер СИЛЬНО тримає target. Не інвалідував таймер — VC живе вічно.",
        "Батько-дитина: батько тримає дітей strong, дитина батька — weak (view ієрархії, координатори).",
        "[weak self] потрібен, коли closure зберігається іншим обʼєктом і буде викликаний пізніше: cell.onTap, viewModel.onUpdate, service.completion — усі callback-и, що живуть довше за функцію, яка їх створила.",
        "[weak self] зазвичай НЕ потрібен: map / filter / sorted, UIView.animate, разовий DispatchQueue.main.async — closure виконується один раз і звільняється; а якщо self у closure не згадується, захоплювати взагалі нічого.",
        "Шпаргалка по UIKit: звичайна властивість — strong; delegate — weak; parent → child — strong; child → parent — weak; self у збереженому closure — weak (рідше unowned); Array/Dictionary/Set обʼєктів — strong.",
      ],
      code: [
        "final class OrderVC: UIViewController {",
        "    var timer: Timer?",
        "    override func viewDidLoad() {",
        "        super.viewDidLoad()",
        "        timer = Timer.scheduledTimer(withTimeInterval: 5, repeats: true) { [weak self] _ in",
        "            self?.refreshStatus()     // block-based API + weak — циклу немає",
        "        }",
        "    }",
        "    deinit { timer?.invalidate(); print(\"OrderVC deinit\") } // лог = перевірка ліків",
        "}",
        "",
        "// Цикл через callback і його розрив",
        "final class DetailVC: UIViewController { var onLike: (() -> Void)? }",
        "detail.onLike = { self.reloadData() }                  // ❌ self → detail → closure → self",
        "detail.onLike = { [weak self] in self?.reloadData() }  // ✅ weak розриває коло",
        "// після виходу з функції detail не вмирає — його тримає UINavigationController",
      ],
      traps: [
        "weak тільки для класів (reference-типів) — на struct не повісиш.",
        "NotificationCenter.addObserver(forName:...) з closure повертає токен — його треба зберігати і знімати; сам closure легко захоплює self.",
        "unowned виглядає «чистіше» (без optional), але один неправильний прогноз часу життя = креш у проді. Сумніваєшся — weak.",
        "Пошук ліків: Debug Memory Graph (кнопка в Xcode) показує, ХТО тримає обʼєкт; Instruments → Leaks/Allocations; найпростіше — print у deinit.",
        "weak не можна оголосити для локальної змінної або параметра функції — тільки для властивості класу.",
        "Після [weak self] тип стає Optional: або self?.method(), або guard let self else { return } першим рядком.",
      ],
      qs: [
        {
          q: "Як ARC відрізняється від garbage collection — плюси й мінуси?",
          a: [
            "ARC — робота компілятора: він вставляє retain/release у код, і обʼєкт помирає детерміновано, у момент, коли зникло останнє сильне посилання. Немає окремого потоку збирача, немає пауз, менше памʼяті «про запас».",
            "Ціна — цикли: ARC не вміє їх знаходити, це відповідальність розробника (weak/unowned). GC цикли збирає сам, але платить паузами і непередбачуваним моментом звільнення.",
          ],
        },
        {
          q: "Таблиця strong/weak/unowned: чи тримає, чи optional, що при звертанні до мертвого?",
          a: [
            "strong: тримає, збільшує лічильник, обʼєкт живий поки посилання існує — саме тут можливий цикл.",
            "weak: не тримає, завжди Optional var, після смерті обʼєкта автоматично стає nil. unowned: не тримає, не Optional, після смерті обʼєкта звертання = краш.",
          ],
        },
        {
          q: "Чому delegate — weak? Що станеться, якщо зробити strong?",
          a: [
            "Бо власник тут — той, хто створив обʼєкт і показує його: контролер тримає tableView або дитячий VC сильно. Якщо дитина ще й сильно триматиме делегата-батька — вийде коло, і жоден не звільниться.",
            "Симптом strong-делегата у проді: екран закрився, а контролер живий — далі приходять нотифікації, оновлюються дані, іноді два обробники на одну подію. Тому такі протоколи ще й обмежують AnyObject, щоб weak був можливий.",
          ],
        },
        {
          q: "Як ти шукав/шукав би memory leak у реальному проєкті — покроково?",
          a: [
            "1) print у deinit — переконатись, що екран справді не звільняється. 2) Debug Memory Graph у Xcode — він показує, ХТО саме тримає обʼєкт. 3) Instruments (Leaks/Allocations), якщо тече не контролер, а щось глибше.",
            "Далі перевіряю типових підозрюваних: збережений closure без weak, strong delegate, таймер без invalidate, спостерігач без відписки, дитячий VC без removeFromParent.",
          ],
        },
        {
          q: "Що означає «власник» обʼєкта і як із цього правила виводиться, де ставити weak?",
          a: [
            "Власник — той, хто відповідає за час життя обʼєкта, і саме він тримає strong. Далі правило механічне: якщо посилання не про власність — воно weak (або unowned).",
            "Звідси і delegate weak, і child → parent weak, і [weak self] у збереженому callback: жоден із них не володіє тим, на що дивиться.",
          ],
        },
        {
          q: "Чому weak не можна оголосити для локальної змінної або параметра функції?",
          a: [
            "weak — це властивість зі спеціальним сховищем, яке ARC реєструє, щоб обнулити його після смерті обʼєкта. Локальні змінні й параметри живуть у межах виклику і такого сховища не мають.",
            "Практично воно й не потрібне: поки функція працює, обʼєкт і так утримується сильно.",
          ],
        },
        {
          q: "Хто утримує DetailViewController після того, як функція, яка його створила, завершилась?",
          a: [
            "UINavigationController: pushViewController додає контролер у стек і тримає його сильно. Локальна змінна vc зникає, обʼєкт — ні.",
            "Тому він живе до pop; і саме тому цикл через self у його callback тримає ще й батьківський екран.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "GCD і класична багатопотоковість",
      what: [
        "GCD (Grand Central Dispatch) — API черг задач поверх пулу потоків. Serial черга виконує задачі по одній у порядку додавання; concurrent — кілька одночасно. main — serial черга головного потоку (весь UI). Global-черги — concurrent, з рівнями QoS: userInteractive > userInitiated > utility > background — система розподіляє ресурси за пріоритетом.",
        "async — «поклади задачу і не чекай»; sync — «поклади і заблокуйся до завершення». DispatchGroup — дочекатися групи задач; barrier — задача-«шлюз» у concurrent черзі: чекає всі попередні, виконується сама, потім пускає наступні (патерн readers-writers).",
        "Застосунок починає життя на Main Thread: main() → UIApplicationMain → AppDelegate → SceneDelegate → UIWindow → перший ViewController → viewDidLoad. Main Thread (він же UI Thread; main queue — логічна черга на ньому) відповідає за весь інтерфейс: малювання, натискання, жести, анімації, скрол списків, переходи, Auto Layout. Звідси правило Apple: UI змінюємо ЛИШЕ на main, а важку роботу (мережа, JSON, диск, база, обробка фото, обчислення) виносимо у фонові черги.",
        "DispatchQueue ≠ Thread — типова плутанина новачків: черга це лише список задач, потік — місце, де вони реально виконуються, а який саме потік узяти, вирішує GCD. Усі черги працюють за FIFO, але це гарантія лише порядку СТАРТУ: у concurrent черзі задачі можуть завершитися у будь-якому порядку.",
      ],
      problem: "Вирішує дві проблеми: (1) не блокувати main thread важкою роботою — інакше фризи UI і креш від watchdog; (2) керувати паралелізмом без ручного створення потоків. Race condition — коли результат залежить від порядку виконання потоків; data race — одночасний неатомарний доступ до памʼяті (мінімум один запис) — це UB, ловиться Thread Sanitizer.",
      where: [
        "Класичний сендвіч: DispatchQueue.global().async { важка робота } → DispatchQueue.main.async { оновити UI }.",
        "Декодування великого JSON, ресайз картинок, робота з диском — усе поза main.",
        "DispatchGroup: зібрати 3 запити (меню + промо + профіль) і показати екран, коли ВСІ готові — твій реальний кейс на home page.",
        "Serial черга як памʼятка синхронізації: усі звертання до спільного кеша через одну serial чергу = потокобезпечність без локів.",
        "QoS за призначенням: .userInteractive — анімації, жести, миттєвий відгук UI; .userInitiated — користувач уже чекає результат (пошук, відкриття документа, запит після тапу; найчастіший вибір); .default — середній, коли не вказано; .utility — довге, про що юзер знає і не поспішає (імпорт, експорт, бекап, завантаження відео); .background — те, чого юзер не помічає (очищення кешу, синхронізація, індексація, аналітика).",
        "Тільки на main: label.text, imageView.image, tableView.reloadData(), view.backgroundColor — будь-яка зміна UI. Якщо є дві задачі одночасно, .userInteractive отримає ресурси раніше за .background — саме тому інтерфейс лишається плавним.",
      ],
      code: [
        "let group = DispatchGroup()",
        "var menu: Menu?; var promos: [Promo] = []",
        "group.enter(); api.fetchMenu   { menu = $0;   group.leave() }",
        "group.enter(); api.fetchPromos { promos = $0; group.leave() }",
        "group.notify(queue: .main) { self.render(menu, promos) }",
        "",
        "// Потокобезпечний доступ: concurrent + barrier",
        "queue.async(flags: .barrier) { self.cache[key] = value }  // запис — ексклюзивно",
        "queue.sync { return self.cache[key] }                     // читання — паралельно",
        "",
        "// Типовий сендвіч: важка робота у фоні → повернення на main",
        "DispatchQueue.global(qos: .userInitiated).async {",
        "    guard let data = try? Data(contentsOf: url) else { return }  // мережа/диск",
        "    let users = self.parse(json: data)                           // парсинг",
        "    DispatchQueue.main.async {",
        "        self.users = users",
        "        self.tableView.reloadData()                              // UI — тільки main",
        "    }",
        "}",
      ],
      traps: [
        "DispatchQueue.main.sync { } з main thread = миттєвий deadlock: main чекає задачу, яка чекає main. Питання №1 на співбесідах.",
        "sync на будь-якій serial черзі зсередини неї самої — той самий deadlock.",
        "Забутий group.leave() (наприклад, у гілці помилки) — notify не спрацює ніколи.",
        "GCD не скасовує задачі, що вже виконуються; DispatchWorkItem.cancel() — лише прапорець isCancelled. Потрібне справжнє скасування/залежності — OperationQueue або Swift Concurrency.",
        "Зміна UI із фонової черги не завжди падає одразу: буває попередження Xcode, дивна верстка і випадковий креш пізніше — саме той баг, що «не відтворюється».",
        "Важка робота на main = «додаток завис»: кнопки не тиснуться, скрол стоїть, анімації стоять — далі watchdog вбиває процес.",
        "Усередині closure Swift вимагає явний self — це не примха компілятора, а підказка, що closure захоплює обʼєкт.",
        "[weak self] у GCD зазвичай не потрібен (closure виконується один раз і звільняється), але потрібен для довгих задач, якщо екран може закритися раніше: async { [weak self] in guard let self else { return } … }.",
      ],
      qs: [
        {
          q: "Що станеться при DispatchQueue.main.sync {} з main thread — і чому саме?",
          a: [
            "Миттєвий deadlock. sync блокує поточний потік до завершення задачі, а виконатись вона може лише на main — який щойно заблокував сам себе.",
            "Те саме буде на будь-якій serial черзі, якщо викликати sync зсередини неї самої.",
          ],
        },
        {
          q: "Serial vs concurrent, sync vs async — чотири комбінації, поясни кожну.",
          a: [
            "Serial/concurrent — властивість ЧЕРГИ: по одній задачі за раз чи кілька одночасно. sync/async — властивість ВИКЛИКУ: чекати завершення чи ні.",
            "serial+async — покласти і йти далі, порядок збережеться (типова синхронізація стану). serial+sync — покласти й чекати (класичний спосіб отримати deadlock). concurrent+async — паралельно і не чекаючи, звичайний фон. concurrent+sync — чекати свій результат, поки решта черги працює паралельно (читання у readers-writers).",
          ],
        },
        {
          q: "Як завантажити 3 ресурси паралельно і оновити UI, коли всі готові — GCD і async/await варіанти?",
          a: [
            "GCD: DispatchGroup — enter перед кожним запитом, leave у колбеку, notify(queue: .main) для рендера. Головна пастка — забути leave у гілці помилки, тоді notify не спрацює ніколи.",
            "Swift Concurrency: async let на три виклики і один await для всіх трьох (або withTaskGroup, якщо кількість динамічна) — коротше, з нормальною обробкою помилок і скасуванням.",
          ],
          code: [
            "async let menu = api.menu()",
            "async let promos = api.promos()",
            "async let profile = api.profile()",
            "let (m, p, pr) = try await (menu, promos, profile)",
            "render(m, p, pr)                 // уже на MainActor",
          ],
        },
        {
          q: "Race condition vs data race — різниця? Як ловити (TSan)?",
          a: [
            "Data race — технічна річ: два потоки звертаються до однієї памʼяті без синхронізації і хоча б один із них пише. Це undefined behavior.",
            "Race condition — логічна: результат залежить від того, хто встиг перший, навіть якщо кожен окремий доступ безпечний. Data race ловить Thread Sanitizer (схема → Diagnostics), race condition — ні: її видно лише з логіки й тестів.",
          ],
        },
        {
          q: "Чим DispatchQueue відрізняється від Thread — і хто вирішує, на якому потоці виконається задача?",
          a: [
            "Черга — це список задач, потік — місце, де вони виконуються. Ти кладеш задачу в чергу, а потік із пулу підбирає GCD.",
            "Виняток — main queue: вона завжди виконується на головному потоці. Для решти покладатися на конкретний потік не можна.",
          ],
        },
        {
          q: "FIFO гарантує порядок старту чи порядок завершення? Що це означає для concurrent черги?",
          a: [
            "Тільки порядок старту. У serial черзі це водночас і порядок завершення, бо задачі не перекриваються.",
            "У concurrent черзі три задачі стартують по черзі, а завершитись можуть як завгодно — тому результати збирають через DispatchGroup або барʼєр, а не покладаються на порядок.",
          ],
        },
        {
          q: "Назви всі пʼять QoS і по одному реальному прикладу до кожного.",
          a: [
            ".userInteractive — анімації й жести; .userInitiated — юзер натиснув і чекає (пошук, відкриття документа); .default — коли QoS не вказали; .utility — довге, про що юзер знає (експорт, бекап, завантаження відео); .background — те, чого юзер не бачить (очищення кешу, синхронізація, аналітика).",
            "Сенс не в назвах, а в тому, що система розподіляє процесорний час за пріоритетом: .userInteractive виграє в .background — саме тому інтерфейс лишається плавним.",
          ],
        },
        {
          q: "Що станеться, якщо змінити label.text із DispatchQueue.global() — і чому це найгірший вид бага?",
          a: [
            "UIKit не потокобезпечний: буде або попередження Main Thread Checker, або зламана верстка, або краш — і не обовʼязково зараз.",
            "Найгірший саме тому, що недетермінований: у дебазі працює, у користувача падає раз на тисячу запусків. Тому будь-яку зміну UI загортаємо в DispatchQueue.main.async.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Swift Concurrency: async/await, actors, Sendable",
      also: ["swift-18", "swift-20"],
      what: [
        "async/await — синтаксис для асинхронності без колбеків: await позначає suspension point, де функція може «заснути», звільнивши потік для іншої роботи (кооперативний пул потоків ≈ кількість ядер; потоки не блокуються). Task { } — міст із синхронного світу в асинхронний; structured concurrency (async let, TaskGroup) — дочірні задачі не переживають батьківську область, скасування поширюється автоматично.",
        "actor — reference-тип, який серіалізує доступ до свого стану: одночасно всередині виконується лише один виклик, data race неможливий за визначенням. @MainActor — глобальний актор головного потоку: вішається на клас/метод, і компілятор гарантує виконання на main. Sendable — маркер «тип безпечно передавати між потоками»; у Swift 6 strict concurrency компілятор перевіряє це на етапі збірки.",
      ],
      problem: "Вирішує: (1) callback hell і забуті completion(...) у гілках помилок — лінійний код з try/await читається як синхронний; (2) data races — actors та Sendable переносять помилки конкурентності з рантайму (найважчі в дебагу баги) у compile time; (3) скасування — Task.cancel() поширюється деревом задач, GCD такого не має.",
      where: [
        "Мережевий шар: let (data, response) = try await session.data(for: request) — сучасний стандарт, у тебе на роботі так.",
        "Паралельні незалежні запити: async let menu = api.menu(); async let promos = api.promos(); let page = try await (menu, promos).",
        "@MainActor на ViewModel — усі @Published/стан оновлюються на main без ручних DispatchQueue.main.async.",
        "AsyncStream — обгортка над делегатами/нотифікаціями: стрім оновлень геолокації, статусів замовлення (твої Live Activities поруч із цим).",
        "withCheckedThrowingContinuation — обгортання старого callback-API (Firebase, сторонні SDK) в async.",
      ],
      code: [
        "func loadImages(urls: [URL]) async throws -> [UIImage] {",
        "    try await withThrowingTaskGroup(of: UIImage.self) { group in",
        "        for url in urls {",
        "            group.addTask { try await self.download(url) }   // паралельно",
        "        }",
        "        var result: [UIImage] = []",
        "        for try await image in group { result.append(image) }",
        "        return result",
        "    }",
        "}",
        "",
        "actor ImageCache {",
        "    private var storage: [URL: UIImage] = [:]",
        "    func image(for url: URL) -> UIImage? { storage[url] }   // доступ серіалізовано",
        "    func insert(_ img: UIImage, for url: URL) { storage[url] = img }",
        "}",
      ],
      traps: [
        "Actor reentrancy: під час await усередині actor-метода інший виклик може зайти і змінити стан — інваріанти перевіряй ПІСЛЯ await заново.",
        "Task { } успадковує актор-контекст місця створення; Task.detached — ні (і пріоритет теж). Detached — майже завжди неправильний вибір.",
        "Скасування кооперативне: якщо код не перевіряє Task.isCancelled / не викликає try Task.checkCancellation() — він виконається до кінця попри cancel().",
        "async let без await результату — задача все одно виконається і буде скасована при виході з області; try await обовʼязково для throwing.",
        "Блокувати потік з async-контексту (semaphore.wait, sleep) — забирає потік з кооперативного пулу, ризик starvation.",
      ],
      qs: [
        {
          q: "Чим async let відрізняється від послідовних await і від TaskGroup — коли що?",
          a: [
            "Послідовні await — строго один за одним: другий запит стартує лише після завершення першого, і час додається. async let запускає задачу одразу на місці оголошення, паралельно з наступними рядками, а await на змінній лише забирає готовий результат.",
            "Різниця async let і TaskGroup — у кількості й типах. async let для фіксованого набору, відомого на компіляції: меню, промо, профіль — різні типи, різні змінні. TaskGroup — коли задач N у рантаймі й вони однотипні: масив URL картинок, сторінки пагінації.",
            "Обидва — structured concurrency: дочірні задачі не переживають область, скасування батька йде вниз. Пастка: async let без await усе одно виконається, а на виході з області буде скасована; для throwing-виклику потрібен саме try await.",
          ],
          code: [
            "async let menu = api.menu()          // стартує одразу",
            "async let promos = api.promos()      // паралельно",
            "let page = try await Page(menu: menu, promos: promos)",
            "",
            "// N задач, відомих лише в рантаймі",
            "let images = try await withThrowingTaskGroup(of: UIImage.self) { group in",
            "    for url in urls { group.addTask { try await download(url) } }",
            "    return try await group.reduce(into: [UIImage]()) { $0.append($1) }",
            "}",
          ],
        },
        {
          q: "Що таке actor reentrancy і який баг вона може створити? Приклад.",
          a: [
            "Actor гарантує, що одночасно всередині виконується один виклик, але не гарантує, що виклик пройде цілим. На кожному await актор відпускається, і в ту саму інстанцію заходить інший виклик — і бачить стан, який ще не догнав твою логіку.",
            "Класичний баг — подвійне завантаження в кеші: перевірив storage[url], там nil, пішов await download. Поки качається, другий виклик з тим самим url теж бачить nil і теж качає. Два мережеві запити замість одного, і другий результат перетирає перший.",
            "Лікується не локом, а словником in-flight задач: другий виклик підхоплює вже наявний Task і чекає його. Загальне правило — інваріант, перевірений до await, після await перечитуй заново.",
          ],
          code: [
            "actor ImageCache {",
            "    private var storage: [URL: UIImage] = [:]",
            "    private var inFlight: [URL: Task<UIImage, Error>] = [:]",
            "    func image(for url: URL) async throws -> UIImage {",
            "        if let cached = storage[url] { return cached }",
            "        if let task = inFlight[url] { return try await task.value }",
            "        let task = Task { try await download(url) }",
            "        inFlight[url] = task",
            "        let image = try await task.value   // тут актор відпущено",
            "        storage[url] = image",
            "        inFlight[url] = nil",
            "        return image",
            "    }",
            "}",
          ],
        },
        {
          q: "Чим Task { } відрізняється від Task.detached — і чому detached майже завжди неправильний вибір?",
          a: [
            "Task { } успадковує контекст місця створення: актор (зокрема MainActor), пріоритет і task-local значення. Тобто всередині ти лишаєшся там, де був, і скасування долітає зверху, якщо задача дочірня.",
            "Task.detached не успадковує нічого — ні актора, ні пріоритету. Код, який щойно був на MainActor, раптом виконується на пулі, і оновлення UI усередині вже вимагає явного повернення на головний актор.",
            "Тому detached виправданий лише для справді незалежної фонової роботи без звʼязку з контекстом — запис аналітики, прогрів кешу. У решті випадків правильна відповідь — звичайний Task із явним пріоритетом.",
          ],
          code: [
            "@MainActor func reload() {",
            "    Task {                       // лишається на MainActor",
            "        let items = try? await api.menu()",
            "        self.render(items)       // UI — без стрибків між акторами",
            "    }",
            "    Task.detached {              // контекст втрачено",
            "        await analytics.log(\"reload\")",
            "    }",
            "}",
          ],
        },
        {
          q: "Чому await у actor-методі — «діра» для зміни стану і як з цим жити?",
          a: [
            "Бо await — це suspension point: актор на цей час відпускає свою чергу і впускає інші виклики. Усе, що ти прочитав або перевірив ДО await, після нього може бути іншим. Ізоляція захищає від data race, але не робить метод атомарним.",
            "Як жити: тримай читання і запис стану в одній синхронній ділянці, без await між ними. Якщо await неминучий — після нього перечитуй властивість заново, а не користуйся локальною копією, і перевіряй, чи хтось уже не зробив цю роботу за тебе.",
            "Структурний хід — витягнути довгу роботу за межі актора: актор лише віддає й приймає стан, а мережа й декодинг живуть зовні. Тоді всередині немає await, і питання реентрантності просто не виникає.",
          ],
          code: [
            "actor OrderStore {",
            "    private var items: [Order] = []",
            "    func refresh() async throws {",
            "        guard items.isEmpty else { return }        // перевірили",
            "        let fresh = try await api.orders()         // актор відпущено",
            "        guard items.isEmpty else { return }        // перечитали заново",
            "        items = fresh",
            "    }",
            "}",
          ],
        },
        {
          q: "Що перевіряє Swift 6 strict concurrency і що таке Sendable?",
          a: [
            "Sendable — маркерний протокол «значення цього типу безпечно передавати між ізоляційними доменами». Struct і enum із Sendable-полів отримують його автоматично; клас — лише якщо final і всі поля let, або якщо він сам синхронізує доступ; актори Sendable за побудовою.",
            "Strict concurrency переносить перевірку в compile time: компілятор знає, який код у якій ізоляції, і не дає ні передати не-Sendable тип через межу, ні звернутися до @MainActor-стану з фонової задачі. Помилка конкурентності стає помилкою збірки, а не плаваючим багом у проді.",
            "На міграції болять старі мутабельні класи-моделі й типи, які ніхто не позначав. Реальні ходи: перевести модель у struct, повісити @MainActor на UI-шар, а @unchecked Sendable ставити лише там, де синхронізацію ти справді забезпечив — це обіцянка компілятору, а не перевірка.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Protocols і Protocol-Oriented Programming",
      what: [
        "Протокол — контракт вимог (методи, властивості, associated types). Protocol extension дає реалізації за замовчуванням — так будується POP: спільна поведінка через композицію протоколів замість ієрархії класів. Структури можуть підкорятися багатьом протоколам — «множинне успадкування поведінки» без класів.",
        "some Protocol (opaque type) — конкретний тип, відомий компілятору, але прихований від читача: статичний dispatch, нуль накладних витрат. any Protocol (existential) — «коробка», у якій може лежати будь-який тип, що підкоряється: динамічний dispatch, боксинг, повільніше. Правило: some де можливо, any де потрібна справжня гетерогенність.",
      ],
      problem: "Вирішує жорсткість класового успадкування: один базовий клас, глибокі ієрархії, «god object» нагорі. І головне для практики — розвʼязування залежностей: код залежить від абстракції (протоколу), а не конкретного типу, що робить його тестованим (підміна мока) і замінюваним (переїхали з одного SDK на інший — переписали одну реалізацію).",
      where: [
        "DI + тестування — головний практичний кейс: protocol OrderServiceProtocol { func fetchOrders() async throws -> [Order] }; ViewModel приймає протокол, у тестах — MockOrderService. Це закриває твій геп у тестуванні.",
        "Твій мережевий шар: типізовані запити — це протокол Endpoint з associated type відповіді.",
        "Delegate-патерн UIKit — протоколи (UITableViewDataSource).",
        "Стандартна бібліотека: Equatable, Hashable, Codable, Sequence — усе протоколи; кастомний тип у Set вимагає Hashable.",
      ],
      code: [
        "protocol OrderService {",
        "    func fetchOrders() async throws -> [Order]",
        "}",
        "struct APIOrderService: OrderService { /* реальні запити */ }",
        "struct MockOrderService: OrderService {",
        "    var stub: [Order] = []",
        "    func fetchOrders() async throws -> [Order] { stub }",
        "}",
        "// Пастка static dispatch у extension:",
        "protocol Greeter { }               // метод НЕ оголошений у протоколі",
        "extension Greeter { func hi() -> String { \"proto\" } }",
        "struct Kyiv: Greeter { func hi() -> String { \"kyiv\" } }",
        "let g: Greeter = Kyiv()",
        "g.hi()   // \"proto\"! — метод не в вимогах → static dispatch за типом змінної",
      ],
      traps: [
        "Класика-пастка (у коді вище): метод, оголошений ТІЛЬКИ в extension протоколу, диспатчиться статично за типом змінної. Додай метод у вимоги протоколу — стане динамічним і поверне \"kyiv\".",
        "any Protocol має ціну: existential container, непрямі виклики. У гарячих місцях і колекціях краще generic constraint.",
        "Протокол з associated type до Swift 5.7 не можна було використовувати як тип — тільки як constraint; тепер any/some це помʼякшили (primary associated types).",
        "Протокол для делегата має бути AnyObject (class-only) — інакше weak не повісиш.",
      ],
      qs: [
        {
          q: "Пастка: метод тільки в extension протоколу — що викличеться через змінну типу протоколу і чому?",
          a: [
            "Викличеться реалізація з extension протоколу, а не з конкретного типу. Причина — методу немає у вимогах протоколу, тож немає і в witness table: компілятор нічого не шукає в рантаймі, він статично звʼязує виклик за типом змінної.",
            "У прикладі з Greeter змінна типу протоколу дає proto, хоча в коробці лежить Kyiv зі своїм hi(). Присвой той самий обʼєкт у змінну типу Kyiv — отримаєш kyiv. Тобто результат залежить від того, як оголошена змінна, а не від того, що в ній.",
            "Лікування одне: оголоси метод у вимогах протоколу. Тоді він потрапляє у witness table, dispatch стає динамічним, а тіло в extension лишається лише дефолтом для тих типів, які його не реалізували.",
          ],
          code: [
            "protocol Greeter { }                      // метод НЕ у вимогах",
            "extension Greeter { func hi() -> String { \"proto\" } }",
            "struct Kyiv: Greeter { func hi() -> String { \"kyiv\" } }",
            "",
            "let g: Greeter = Kyiv()",
            "g.hi()        // \"proto\" — static dispatch за типом змінної",
            "Kyiv().hi()   // \"kyiv\"",
            "",
            "protocol Greeter2 { func hi() -> String }  // тепер у вимогах",
            "// → witness table → \"kyiv\" в обох випадках",
          ],
        },
        {
          q: "some vs any: різниця в dispatch, перформансі, можливостях? Чому SwiftUI повертає some View?",
          a: [
            "some — opaque type: конкретний тип рівно один і компілятору відомий, просто прихований від того, хто викликає. Звідси статичний dispatch, спеціалізація, нуль накладних витрат. any — existential: коробка, у якій може лежати будь-який відповідний тип, виклики йдуть через witness table.",
            "Тому й можливості різні. [any Shape] легально — там справді різні типи; some Shape так не складеш, він мусить бути одним і тим самим на всіх шляхах return. Ціна any — непрямі виклики плюс боксинг у heap, якщо значення не влазить у буфер контейнера.",
            "SwiftUI повертає some View, бо тип body — це велетенський вкладений generic на кшталт VStack<TupleView<...>>. Саме за цим типом фреймворк розуміє структуру дерева й оновлює його на місці. Загорнеш у AnyView — інформація зникне, і замість оновлення отримаєш перестворення.",
          ],
        },
        {
          q: "Як протоколи роблять ViewModel тестованим — покажи на прикладі мока мережі.",
          a: [
            "ViewModel залежить не від конкретного APIClient, а від протоколу, і отримує реалізацію через init. У проді туди йде справжній сервіс, у тесті — мок, який віддає готові дані або кидає помилку. Мережа з тесту зникає: він швидкий, офлайновий і детермінований.",
            "Що це дозволяє перевірити: на успіху стан переходить у loaded із правильними даними, на 401 — у failed, спінер знімається в обох випадках. Мок ще й рахує виклики, тому тестом ловиться дубльований запит або зайвий refresh.",
            "Практичний момент: протокол описує рівно те, що потрібно ViewModel — один-два методи, а не весь клієнт. Тоді мок пишеться в пʼять рядків, і тест не ламається від кожної зміни в мережевому шарі.",
          ],
          code: [
            "protocol OrderService {",
            "    func fetchOrders() async throws -> [Order]",
            "}",
            "struct MockOrderService: OrderService {",
            "    var stub: Result<[Order], Error> = .success([])",
            "    func fetchOrders() async throws -> [Order] { try stub.get() }",
            "}",
            "",
            "let vm = OrderViewModel(",
            "    service: MockOrderService(stub: .failure(APIError.unauthorized))",
            ")",
            "await vm.load()",
            "// XCTAssertEqual(vm.state, .failed(message: \"...\"))",
          ],
        },
        {
          q: "POP vs успадкування: наведи приклад рефакторингу ієрархії на протоколи.",
          a: [
            "Успадкування дає лише один базовий клас і тягне за собою все його майно. Типова хвора точка — BaseViewController, у якому спінер, алерти, аналітика й pull-to-refresh разом; кожен екран успадковує все, використовує третину, а зміна в базі зачіпає двадцять екранів.",
            "Рефакторинг: розрізати базу на здатності — protocol LoadingPresenting, ErrorPresenting, AnalyticsTracking, у кожного extension із дефолтною реалізацією. Екран замовлень підписується на два потрібні, екран меню — на інші. Дублювання не зʼявилось, а вертикальної ієрархії більше немає.",
            "Формулювання для співбесіди: успадкування про «є різновидом», протокол про «вміє». Плюс struct і enum можуть підкорятися протоколам, а від класу успадкуватись не можуть — тому POP працює і там, де value-типи.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Generics",
      what: [
        "Generics — параметризація коду типами: func max<T: Comparable>(_ a: T, _ b: T) -> T працює для будь-якого порівнюваного типу, зберігаючи типобезпеку (на виході саме T, не Any). Constraints (T: Codable, where Element == String) звужують можливості до потрібних. Компілятор спеціалізує generic-код під конкретні типи — на виході швидкий мономорфний код без боксингу.",
        "Conditional conformance: «масив Equatable-елементів сам Equatable» — extension Array: Equatable where Element: Equatable. Primary associated types (Swift 5.7): some Collection<String>.",
      ],
      problem: "Вирішує дилему «дублювати код під кожен тип чи втратити типи через Any». До generics контейнери або писались під один тип, або повертали Any з кастами в рантаймі (і крешами). Generics дають одночасно перевикористання І compile-time безпеку І перформанс.",
      where: [
        "Твій мережевий шар — найкращий приклад на співбесіді: func request<T: Decodable>(_ endpoint: Endpoint) async throws -> T — один метод декодує будь-яку модель.",
        "Уся стандартна бібліотека: Array<Element>, Dictionary<Key, Value>, Optional<Wrapped>, Result<Success, Failure>.",
        "Generic-репозиторій/сховище: save<T: Encodable>(_ value: T, key: String).",
        "SwiftUI повсюдно: ForEach<Data, ID, Content>, Button<Label>.",
      ],
      code: [
        "struct Endpoint<Response: Decodable> {",
        "    let path: String",
        "    let method: HTTPMethod",
        "}",
        "extension Endpoint {",
        "    static var orders: Endpoint<[Order]> { .init(path: \"/orders\", method: .get) }",
        "}",
        "func request<R>(_ endpoint: Endpoint<R>) async throws -> R {",
        "    let (data, _) = try await session.data(for: makeURLRequest(endpoint))",
        "    return try decoder.decode(R.self, from: data)",
        "}",
        "let orders = try await request(.orders)   // тип виведено: [Order]",
      ],
      traps: [
        "Generic-параметр фіксується на момент виклику — не можна в один [T] покласти різні типи (для цього existential any).",
        "Занадто загальні констрейнти → нечитабельні сигнатури; іноді простіше два перевантаження.",
        "Стирання типів: коли треба зберегти «будь-який Endpoint» у масиві — потрібен type erasure або any.",
      ],
      qs: [
        {
          q: "Напиши generic-функцію пошуку максимального елемента з constraint.",
          a: [
            "Ключове тут — constraint: без T: Comparable оператор > просто недоступний, компілятор не знає, що елементи можна порівнювати. Для колекції правильна сигнатура — generic по Sequence з Element: Comparable і поверненням Optional, бо в порожньої послідовності максимуму немає.",
            "Загальніший варіант — приймати KeyPath або компаратор: тоді функція працює і для типів, які не Comparable, наприклад максимум по createdAt. Стандартна бібліотека зроблена саме так: max() поруч із max(by:).",
          ],
          code: [
            "func maxElement<S: Sequence>(_ items: S) -> S.Element?",
            "    where S.Element: Comparable {",
            "    var result: S.Element?",
            "    for item in items {",
            "        if let current = result, item <= current { continue }",
            "        result = item",
            "    }",
            "    return result",
            "}",
            "",
            "// без Comparable — за KeyPath",
            "func maxElement<S: Sequence, V: Comparable>(",
            "    _ items: S, by key: KeyPath<S.Element, V>",
            ") -> S.Element? { items.max { $0[keyPath: key] < $1[keyPath: key] } }",
          ],
        },
        {
          q: "Чим generic параметр відрізняється від any Protocol за перформансом (спеціалізація vs existential)?",
          a: [
            "У generic тип відомий на місці виклику, тому компілятор спеціалізує функцію під конкретний тип: прямі виклики, інлайнінг, значення на стеку. any — existential container, де конкретний тип відомий лише в рантаймі, а виклики йдуть непрямо через witness table.",
            "Додається боксинг: у existential значення лежить inline, поки влазить у невеликий буфер контейнера, інакше їде в heap — а це алокація і зайве розіменування на кожному доступі. На гарячому шляху і в довгих колекціях різниця вимірна.",
            "Практичне правило: constraint (T: OrderService) — дефолт, any — коли справді потрібна гетерогенність, наприклад масив різнотипних запитів. Але перформанс не привід ускладнювати сигнатуру там, де виклик один раз на екран.",
          ],
        },
        {
          q: "Розкажи, як влаштований типізований запит у вашому мережевому шарі.",
          a: [
            "Шаблон відповіді (проєкт — застосунок доставки їжі Eatery Club): Endpoint параметризований типом відповіді — struct Endpoint<Response: Decodable> з path, методом і тілом. Конкретні запити описані статичними властивостями: .orders має тип Endpoint<[Order]>, .menu — Endpoint<Menu>.",
            "Клієнт має один generic-метод: request(_ endpoint: Endpoint<R>) async throws -> R. Він збирає URLRequest, стріляє через URLSession, мапить HTTP-код у APIError і декодує R. На виклику тип виводиться сам: let orders = try await client.request(.orders) — без кастів і без decode на місці.",
            "Тут перевіряють, чи ти розумієш, що дає параметризація: неможливо задекодити відповідь у чужу модель, декодинг і обробка помилок живуть в одному місці, а для тестів підміняється весь клієнт на рівні протоколу.",
          ],
          code: [
            "struct Endpoint<Response: Decodable> {",
            "    let path: String",
            "    let method: HTTPMethod",
            "}",
            "extension Endpoint {",
            "    static var orders: Endpoint<[Order]> { .init(path: \"/orders\", method: .get) }",
            "}",
            "func request<R>(_ endpoint: Endpoint<R>) async throws -> R {",
            "    let (data, response) = try await session.data(for: makeRequest(endpoint))",
            "    try validate(response)          // HTTP-код → APIError",
            "    return try decoder.decode(R.self, from: data)",
            "}",
            "let orders = try await request(.orders)   // тип виведено: [Order]",
          ],
        },
        {
          q: "Що таке conditional conformance? Приклад зі стандартної бібліотеки.",
          a: [
            "Це підкорення протоколу з умовою на параметр типу: контейнер conform-иться тільки тоді, коли conform-яться його елементи. Пишеться через where в extension — extension Array: Equatable where Element: Equatable.",
            "Стандартна бібліотека на цьому тримається: два [Order] можна порівняти, бо Order: Equatable; Optional Hashable, коли Wrapped Hashable; словник Codable, коли Codable і ключ, і значення. Без умовності був би вибір «вимагати Equatable від усіх елементів» або «не мати цього взагалі».",
            "Свій приклад: Result — Equatable, коли Equatable і Success, і Failure, тому стан екрана порівнюється в тесті одним XCTAssertEqual. Пастка — умова перевіряється на компіляції: [Any] не Equatable, що б там фактично не лежало.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Enums, pattern matching, error handling",
      also: ["swift-17"],
      what: [
        "Swift enum — повноцінний тип: методи, computed properties, generic-параметри. Associated values прикріплюють дані до кейса — case loaded([Order]), case failed(APIError); switch змушує обробити ВСІ кейси (exhaustiveness) — додав кейс, компілятор покаже всі місця, які треба оновити.",
        "Error handling: функція позначається throws, помилка кидається throw, ловиться do/catch. try? перетворює помилку на nil, try! — на креш. rethrows — функція кидає, лише якщо кидає передане їй closure (map, filter). defer — блок, що виконається при БУДЬ-ЯКОМУ виході з області (кілька defer — у зворотному порядку).",
      ],
      problem: "Enum з associated values вирішує проблему неможливих станів: замість isLoading: Bool + error: Error? + data: [Order]? (де можливі безглузді комбінації «і завантажується, і помилка») — один enum, де стан рівно один. Це прибирає цілий клас багів ще на етапі моделювання.",
      where: [
        "Стан екрана: enum ScreenState { case idle, loading, loaded([Order]), failed(String) } — і SwiftUI/UIKit рендерить switch-ом.",
        "Помилки API: enum APIError: Error { case unauthorized, server(code: Int), decoding } — твій мережевий шар з централізованою обробкою помилок робить саме це.",
        "Маршрути дип-лінків: enum DeepLink { case order(id: String), restaurant(id: String), promo(code: String) } — парсинг URL в enum, далі типобезпечна навігація (твій робочий кейс).",
        "Result<T, E> — там, де async/await недоступний: колбеки, збереження результату для повторної обробки.",
      ],
      code: [
        "enum ScreenState {",
        "    case loading",
        "    case loaded([Order])",
        "    case failed(message: String)",
        "}",
        "func render(_ state: ScreenState) {",
        "    switch state {",
        "    case .loading: showSpinner()",
        "    case .loaded(let orders) where orders.isEmpty: showEmptyView()",
        "    case .loaded(let orders): show(orders)",
        "    case .failed(let message): showError(message)",
        "    }",
        "}",
        "if case .loaded(let orders) = state { print(orders.count) } // один кейс без switch",
      ],
      traps: [
        "@unknown default для системних enum (нові кейси в нових iOS — код не зламається, але warning підкаже).",
        "Indirect enum для рекурсивних структур (дерево категорій меню) — без indirect не скомпілюється (нескінченний розмір).",
        "try? ковтає ПРИЧИНУ помилки — для дебагу продакшн-багів краще do/catch з логуванням.",
        "raw value vs associated value — взаємовиключні; rawValue зручний для парсингу з бекенда (enum Status: String, Codable).",
      ],
      qs: [
        {
          q: "Змоделюй стан екрана через enum — чому це краще за три окремі властивості?",
          a: [
            "Три властивості (isLoading, error, orders) дають вісім комбінацій, з яких осмислені три. Решта — «і спінер, і помилка», «нічого не грузиться, даних немає, помилки немає» — існують у типі, а отже рано чи пізно трапляються на екрані, і ти ловиш це репортом від QA.",
            "Enum робить неможливі стани недосяжними: стан рівно один, дані прикріплені до кейса. У .loaded замовлення точно є, у .failed точно є текст — жодних optional-розгортань у рендері. І switch exhaustive: додав .offline, компілятор одразу показав усі місця, які треба оновити.",
            "Пастка, про яку варто сказати самому: pull-to-refresh поверх уже завантажених даних. Тут або окремий кейс .loaded([Order], isRefreshing: Bool), або прапорець поза enum — але усвідомлено, а не поверненням до трьох незалежних властивостей.",
          ],
          code: [
            "enum ScreenState {",
            "    case loading",
            "    case loaded([Order])",
            "    case failed(message: String)",
            "}",
            "switch state {",
            "case .loading: showSpinner()",
            "case .loaded(let orders) where orders.isEmpty: showEmptyView()",
            "case .loaded(let orders): show(orders)",
            "case .failed(let message): showError(message)",
            "}",
          ],
        },
        {
          q: "Що робить rethrows і де він у стандартній бібліотеці (map)?",
          a: [
            "rethrows означає «я кидаю лише тоді, коли кидає переданий мені closure». Свого throw у функції немає — вона тільки пропускає помилку далі, назовні.",
            "Наслідок видно на виклику: передав звичайний closure — try не потрібен; передав throwing — компілятор вимагає try. Одна сигнатура обслуговує обидва випадки, і не доводиться писати окремо map і tryMap.",
            "У стандартній бібліотеці так зроблені map, filter, compactMap, forEach, reduce, sorted(by:). Тому orders.map(\\.id) обходиться без try, а orders.map { try parse($0) } його вимагає — і перша ж кинута помилка обриває проходження, недороблений масив ти не отримаєш.",
          ],
          code: [
            "func transform<T>(_ items: [Int], _ body: (Int) throws -> T) rethrows -> [T] {",
            "    var result: [T] = []",
            "    for item in items { result.append(try body(item)) }",
            "    return result",
            "}",
            "",
            "transform([1, 2]) { $0 * 2 }              // try не потрібен",
            "try transform([1, 2]) { try parse($0) }   // а тут потрібен",
          ],
        },
        {
          q: "Порядок виконання трьох defer у функції?",
          a: [
            "У зворотному порядку оголошення, LIFO. Оголосив A, B, C — виконаються C, B, A. І виконаються на будь-якому виході з області: return, throw, break.",
            "Важлива деталь: реєструються лише ті defer, до яких потік справді дійшов. Якщо функція кинула помилку до третього defer, він не спрацює — його ще не існувало. Тому defer ставлять одразу після операції, яку він прибирає.",
            "Область — не лише функція: defer в тілі циклу відпрацює на кожній ітерації, у do-блоці — на виході з блоку. Звідси головний практичний кейс: isLoading = true і одразу defer { isLoading = false } — спінер знімається на всіх шляхах, включно з throw.",
          ],
          code: [
            "func run() {",
            "    defer { print(\"A\") }",
            "    defer { print(\"B\") }",
            "    defer { print(\"C\") }",
            "    print(\"body\")",
            "}",
            "// body, C, B, A",
          ],
        },
        {
          q: "Як ти парсиш дип-лінки — розкажи про enum маршрутів.",
          a: [
            "Шаблон відповіді (проєкт — застосунок доставки їжі Eatery Club): один enum DeepLink з кейсами під маршрути — .order(id:), .restaurant(id:), .promo(code:) — і failable init?(url:), який розбирає host, pathComponents і query. Незнайомий лінк дає nil, а не пів-навігації.",
            "Парсинг і навігація розділені: init? відповідає тільки за питання «що прийшло», а координатор робить switch по кейсах і будує стек екранів. Тому парсер накривається юніт-тестами без UI — масив URL і очікуваних кейсів.",
            "Тут перевіряють, чи ти витягнув валідацію з місця відкриття. Варто згадати дві пастки: лінк прилетів до логіну — маршрут кладеться в pending і відтворюється після авторизації; і маршрут, якого стара версія застосунку не знає — вона мусить відкрити головну, а не впасти.",
          ],
          code: [
            "enum DeepLink {",
            "    case order(id: String), restaurant(id: String), promo(code: String)",
            "    init?(url: URL) {",
            "        let parts = url.pathComponents.filter { $0 != \"/\" }",
            "        guard let root = parts.first, let id = parts.dropFirst().first",
            "        else { return nil }",
            "        switch root {",
            "        case \"order\":      self = .order(id: id)",
            "        case \"restaurant\": self = .restaurant(id: id)",
            "        case \"promo\":      self = .promo(code: id)",
            "        default: return nil",
            "        }",
            "    }",
            "}",
          ],
        },
      ],
    },
    {
      p: "P2", name: "Method dispatch: static, table, message",
      what: [
        "Три механізми виклику методу. Static (direct) — адреса відома на компіляції: усі методи struct/enum, final-методи, private, методи в extension. Найшвидший, дозволяє інлайнінг. Table (vtable/witness table) — адреса береться з таблиці за індексом у рантаймі: звичайні методи класів (можливий override), вимоги протоколів. Message dispatch — пошук селектора через Objective-C runtime: @objc dynamic методи; найповільніший, але дає магію — KVO, method swizzling, target-action.",
      ],
      problem: "Розуміння dispatch пояснює «дивну» поведінку (пастка з extension протоколу), чому final/private прискорюють код (компілятор девіртуалізує та інлайнить), і чому KVO вимагає @objc dynamic (KVO в рантаймі підміняє клас і перехоплює сеттер — можливо лише з message dispatch).",
      where: [
        "final class / final func на класах, які не успадковуються — безкоштовна оптимізація і явний намір.",
        "@objc dynamic — коли потрібен KVO (спостереження за AVPlayer.status), UIAppearance.",
        "Пастка protocol extension: метод, який є лише в extension і не оголошений вимогою протоколу, викликається статично — для змінної типу протоколу спрацює реалізація з extension, а не та, що в конкретному типі. Це питання саме про dispatch.",
      ],
      code: [
        "class Animal {",
        "    func sound() { }          // table dispatch — можна override",
        "    final func id() { }       // static — override заборонено, інлайниться",
        "    @objc dynamic var name = \"\" // message — видно KVO і swizzling",
        "}",
        "extension Animal {",
        "    func walk() { }           // static! override у нащадку НЕ спрацює як очікуєш",
        "}",
      ],
      traps: [
        "Метод у extension класу — static dispatch: «перевизначення» у сабкласі не викличеться через посилання на базовий тип.",
        "Той самий ефект у protocol extension без оголошення методу в протоколі.",
        "dynamic без потреби — втрата оптимізацій на рівному місці.",
      ],
      qs: [
        {
          q: "Чому KVO вимагає @objc dynamic — що робить рантайм?",
          a: [
            "Бо KVO — це підміна в Objective-C runtime. Коли ти додаєш спостерігача, рантайм створює на льоту підклас (NSKVONotifying_Foo), перемикає isa обʼєкта на нього і перевизначає сеттери так, щоб навколо запису викликати willChangeValue і didChangeValue.",
            "Перехопити виклик можна лише там, де він іде через objc_msgSend. @objc виставляє член у рантайм, а dynamic забороняє компілятору звʼязати виклик статично чи заінлайнити його. Без цієї пари сеттер піде напряму або через vtable, і підмінений клас цього запису просто не побачить.",
            "Звідси й вимоги: клас — нащадок NSObject, спостережувані властивості — @objc dynamic var. У чисто Swift-коді KVO вже не потрібен: @Published або didSet дають те саме без рантайм-магії. KVO лишається там, де обʼєкт чужий і системний, як AVPlayer.status.",
          ],
        },
        {
          q: "Який dispatch у методу з extension класу і яку проблему це створює?",
          a: [
            "Static, тобто прямий. Методи, оголошені в extension класу, у vtable не потрапляють, тому адреса зашивається на компіляції за типом змінної. Компілятор навіть не дасть позначити такий метод як override у нащадку.",
            "Проблема: нащадок оголошує в своєму extension метод з тією ж назвою, і це не перевизначення, а окремий метод. Через змінну базового типу викличеться реалізація базового класу, хоча обʼєкт фактично нащадок. Поліморфізм тихо не працює, і жодної помилки чи ворнінга немає.",
            "Правило просте: те, що планується перевизначати, оголошуй у тілі класу; extension — для справді фінальної поведінки. Механізм той самий, що й у пастці з protocol extension: немає у вимогах або в тілі класу — немає динамічного dispatch.",
          ],
          code: [
            "class Animal {",
            "    func sound() { print(\"animal\") }        // у тілі класу → vtable",
            "}",
            "extension Animal {",
            "    func walk() { print(\"animal walks\") }   // static dispatch",
            "}",
            "class Dog: Animal { override func sound() { print(\"dog\") } }",
            "extension Dog {",
            "    func walk() { print(\"dog walks\") }      // НЕ override, інший метод",
            "}",
            "let a: Animal = Dog()",
            "a.sound()   // \"dog\"",
            "a.walk()    // \"animal walks\"",
          ],
        },
        {
          q: "Як final допомагає компілятору оптимізувати?",
          a: [
            "final каже: перевизначень не буде. Тому виклик, який мав іти через vtable, компілятор девіртуалізує у прямий, а коротке тіло ще й інлайнить — зникає непрямий стрибок, і відкривається місце для решти оптимізацій навколо.",
            "Такий самий ефект дають private (видимість обмежена файлом, отже всі нащадки відомі) і whole-module optimization, де компілятор бачить увесь модуль і сам робить висновок, що клас ніхто не успадковує. За межами модуля цього висновку вже немає.",
            "Практично виграш скромний і помітний на гарячих шляхах, тому головна цінність final інша — явний намір і захист від випадкового сабкласу. Ставити його за замовчуванням дешево, знімати — коли справді потрібне успадкування.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Колекції та функціональні операції",
      what: [
        "map — перетворити кожен елемент (O(n)); compactMap — map + викинути nil; flatMap — map + сплющити вкладені колекції; filter — відібрати; reduce — згорнути в одне значення; sorted — O(n log n). Ланцюги операцій читаються як опис «що зробити», а не «як ітерувати».",
        "Sequence — можна ітерувати (можливо, одноразово і нескінченно); Collection — багаторазова, з індексами і count. .lazy відкладає обчислення: ланцюг lazy.map.filter не створює проміжних масивів і обчислює лише те, що спожито — виграш на великих даних з first(where:).",
        "Складності: Array — доступ за індексом O(1), insert(at: 0) O(n); Set/Dictionary — contains/lookup O(1) середнє (хешування); Array.contains — O(n).",
      ],
      problem: "Функціональні операції вирішують проблему «шумних» циклів з мутабельними акумуляторами: менше місць для off-by-one і забутих умов, наміри видно одразу. Знання складностей вирішує проблеми перформансу: перевірка входження в Array у циклі — O(n²), яка на 10 000 елементів вже помітна; заміна на Set — O(n).",
      where: [
        "Трансформація DTO → доменні моделі: response.items.map(Order.init).",
        "compactMap для «безпечного» парсингу: strings.compactMap(URL.init) — невалідні просто відпадають.",
        "reduce для агрегації: сума кошика items.reduce(0) { $0 + $1.price * Decimal($1.qty) } — твій CartTotals по суті це робить.",
        "Set для швидких перевірок: обрані ресторани favoriteIDs: Set<String> замість масиву.",
      ],
      code: [
        "let ids = orders.map(\\.id)                       // KeyPath замість closure",
        "let urls = strings.compactMap(URL.init)           // невалідні відпали",
        "let total = cart.reduce(Decimal(0)) { $0 + $1.price }",
        "",
        "// lazy: перший підходящий без обробки всього масиву",
        "let first = hugeArray.lazy",
        "    .map(expensiveTransform)",
        "    .first { $0.isValid }",
      ],
      traps: [
        "flatMap на Optional-масивах історично перейменували в compactMap — на співбесіді люблять питати різницю.",
        "Ланцюг map.filter.map на великому масиві створює проміжні масиви — lazy або один прохід reduce.",
        "Hashable-контракт: рівні обʼєкти зобовʼязані мати рівний hash. Зламаєш (нестабільний hash) — Set/Dictionary поводяться непередбачувано.",
        "Мутація колекції під час ітерації — креш/UB; збирай зміни окремо.",
      ],
      qs: [
        {
          q: "flatMap vs compactMap — різниця та історія?",
          a: [
            "flatMap перетворює і сплющує один рівень вкладеності: масив масивів стає одним масивом. compactMap перетворює і викидає nil: масив рядків через compactMap(Int.init) дає лише ті, що справді числа. Різні задачі — «розгорнути вкладеність» проти «відкинути відсутнє».",
            "Історія: до Swift 4.1 обидва звалися flatMap, бо Optional формально теж контейнер, і варіант із closure, що повертає Optional, сплющував саме його. Це плутало читача, тому перевантаження перейменували в compactMap, а за flatMap лишили тільки послідовності.",
            "На практиці compactMap з failable init — готовий безпечний парсер: strings.compactMap(URL.init) тихо відкидає невалідні рядки. Саме в цьому й пастка: якщо тобі важливо, скільки елементів відпало, порівнюй count до і після й логуй різницю.",
          ],
        },
        {
          q: "Складність contains у Array vs Set — і коли це реально важливо?",
          a: [
            "Array.contains — O(n): лінійний перебір із порівнянням до першого влучання. Set.contains — O(1) у середньому: хеш веде одразу в бакет. Побудувати Set із масиву — O(n) плюс памʼять під хеш-таблицю.",
            "Реально важливо, коли перевірка стоїть у циклі: для кожного з N елементів шукати серед M — це O(N·M). На двох сотнях непомітно, на десятках тисяч (позиції меню проти обраних ресторанів) — це вже фризи скролу. Один Set(favoriteIDs) перед циклом робить із цього O(N+M).",
            "Коли не варто: пʼять-десять елементів і одна перевірка — Array швидший, бо без хешування й алокації. І Set вимагає коректного Hashable: нестабільний або неузгоджений із == hash дає найгірший симптом — елемент у колекції є, але не знаходиться.",
          ],
        },
        {
          q: "Коли lazy-ланцюг виграє, а коли він марний (споживаєш все одно все)?",
          a: [
            "lazy виграє, коли ти не споживаєш увесь результат: first(where:), prefix(10), або дорога трансформація перед фільтром. Тоді map виконується лише для елементів, до яких дійшли, і проміжні масиви не створюються взагалі — ні під map, ні під filter.",
            "Марний, коли результат усе одно матеріалізується повністю: Array(hugeArray.lazy.map(f)) обробить кожен елемент, як і звичайний map, але додасть шар обгорток і непрямих викликів — тобто вийде трохи повільніше, ніж без lazy.",
            "Пастки: lazy-послідовність не кешує, повторний прохід перерахує все заново; closure виконується пізніше, тож захоплений стан може вже змінитись. І в профайлері час трансформації видно не там, де вона написана, а там, де її спожили.",
          ],
        },
        {
          q: "Реалізуй reduce через цикл і суму словника через reduce.",
          a: [
            "reduce — це той самий цикл з акумулятором, тільки акумулятор не мутабельна змінна знадвору, а параметр функції: береш початкове значення і на кожному елементі застосовуєш до нього замикання.",
            "Для словника елемент послідовності — кортеж (key, value), тому сума значень це reduce(0) { $0 + $1.value }, або коротше values.reduce(0, +). Порядок обходу словника не визначений, тож reduce тут годиться для комутативних операцій; якщо збираєш рядок для звіту — спершу sorted.",
          ],
          code: [
            "func myReduce<E, R>(_ items: [E], _ initial: R, _ next: (R, E) -> R) -> R {",
            "    var result = initial",
            "    for item in items { result = next(result, item) }",
            "    return result",
            "}",
            "",
            "let prices = [\"pizza\": 250, \"cola\": 45]",
            "let total = prices.reduce(0) { $0 + $1.value }   // 295",
            "let same  = prices.values.reduce(0, +)           // те саме",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Codable глибоко",
      what: [
        "Codable = Decodable + Encodable. Компілятор синтезує реалізацію автоматично, якщо всі властивості Codable. CodingKeys — enum відповідності імен властивостей ключам JSON; keyDecodingStrategy = .convertFromSnakeCase — глобальна конвертація. Повний контроль — кастомний init(from decoder:): контейнери (keyed/unkeyed/singleValue), nested containers для вкладених структур.",
      ],
      problem: "Вирішує ручний парсинг JSON (гори if let json[\"key\"] as? String — багатослівно і криво з помилками). Codable дає декларативний, типобезпечний парсинг з детальними помилками (DecodingError вкаже точний шлях до поля, що не спарсилось).",
      where: [
        "Кожна модель API у твоєму проєкті. Date через dateDecodingStrategy (.iso8601 або кастомний формуттер — бекенди люблять свої формати).",
        "Поле, що буває String або Int (класика реальних бекендів) — кастомний init з послідовними спробами декодування.",
        "Стійкість до сміття в масивах: обгортка FailableDecodable, щоб один битий елемент не валив увесь список (критично для стрічок/каталогів).",
        "Кеш на диску: JSONEncoder у файл — швидкий офлайн-стан без Core Data.",
      ],
      code: [
        "struct Order: Decodable {",
        "    let id: String",
        "    let total: Decimal",
        "    let promoCode: String?          // немає в JSON → nil, без помилки",
        "    enum CodingKeys: String, CodingKey {",
        "        case id = \"order_id\", total, promoCode = \"promo_code\"",
        "    }",
        "}",
        "// String або Int в одному полі:",
        "struct FlexibleID: Decodable {",
        "    let value: String",
        "    init(from decoder: Decoder) throws {",
        "        let c = try decoder.singleValueContainer()",
        "        if let s = try? c.decode(String.self) { value = s }",
        "        else { value = String(try c.decode(Int.self)) }",
        "    }",
        "}",
      ],
      traps: [
        "Оголосив CodingKeys — автосинтез покриває лише перелічені ключі; пропустив властивість без дефолту — помилка компіляції.",
        "decodeIfPresent vs decode для optional: decode(String?.self) впаде, якщо КЛЮЧА немає; decodeIfPresent — поверне nil.",
        "DecodingError має 4 кейси (typeMismatch, valueNotFound, keyNotFound, dataCorrupted) з context.codingPath — навчись читати, це твій дебаг продакшн-парсингу.",
        "Дефолтні значення при відсутньому ключі автосинтез НЕ дає (var x = 0 не допоможе) — потрібен кастомний init або property wrapper.",
      ],
      qs: [
        {
          q: "JSON з полем String-або-Int — як задекодити? Напиши init(from:).",
          a: [
            "Через singleValueContainer і послідовні спроби: спершу try? decode(String.self), не вийшло — Int і привести до рядка. Це загортається в маленький тип-обгортку, щоб решта моделі лишилася на автосинтезі й не обростала ручним init.",
            "Головне — в останній гілці кинути осмислену помилку, а не підставити значення-заповнювач. DecodingError.typeMismatch із живим codingPath одразу скаже, де саме зламалось; інакше в проді ти побачиш порожній id і не зрозумієш, звідки він.",
            "Якщо гнучке поле лежить усередині обʼєкта, а не на місці скаляра, той самий прийом робиться на keyed-контейнері: два try? decode із тим самим ключем. А коли таких полів пів відповіді, правильна відповідь на співбесіді — домовлятись із бекендом, а не множити обгортки.",
          ],
          code: [
            "struct FlexibleID: Decodable {",
            "    let value: String",
            "    init(from decoder: Decoder) throws {",
            "        let c = try decoder.singleValueContainer()",
            "        if let s = try? c.decode(String.self) { value = s }",
            "        else if let i = try? c.decode(Int.self) { value = String(i) }",
            "        else {",
            "            throw DecodingError.typeMismatch(String.self, .init(",
            "                codingPath: c.codingPath,",
            "                debugDescription: \"Очікував String або Int\"))",
            "        }",
            "    }",
            "}",
          ],
        },
        {
          q: "Як пропустити невалідні елементи масиву, не заваливши весь декодинг?",
          a: [
            "Обгорткою над елементом: тип, який декодує T через try? і тримає результат як Optional. Масив декодується у [Failable<Order>], далі compactMap по value — і один битий обʼєкт більше не валить усю відповідь.",
            "Альтернатива без обгортки — розібрати масив вручну через unkeyedContainer: у циклі try? decode(Order.self), а при невдачі зсунути курсор декодом у порожню структуру. Працює, але обгортка простіша саме тим, що зсувом займається сам декодер.",
            "Практика: усе, що відпало, логуй разом із причиною. Інакше каталог тихо худне, і ніхто не помітить, що бекенд змінив формат. І не тягни цей прийом на критичні дані — краще впасти на замовленні, ніж показати замовлення без суми.",
          ],
          code: [
            "struct Failable<T: Decodable>: Decodable {",
            "    let value: T?",
            "    init(from decoder: Decoder) throws { value = try? T(from: decoder) }",
            "}",
            "struct OrdersResponse: Decodable {",
            "    let items: [Order]",
            "    enum CodingKeys: String, CodingKey { case items }",
            "    init(from decoder: Decoder) throws {",
            "        let c = try decoder.container(keyedBy: CodingKeys.self)",
            "        let raw = try c.decode([Failable<Order>].self, forKey: .items)",
            "        items = raw.compactMap(\\.value)",
            "    }",
            "}",
          ],
        },
        {
          q: "decode vs decodeIfPresent — різниця?",
          a: [
            "decode вимагає, щоб ключ був у JSON: немає — DecodingError.keyNotFound. decodeIfPresent повертає nil, якщо ключа немає або значення null, і не кидає. Різниця не в тому, optional властивість чи ні, а в тому, чи вважається відсутність ключа помилкою.",
            "Ось на чому ловляться: у ручному init пишуть try c.decode(String?.self, forKey: .promoCode), думаючи «тип optional, значить поле необовʼязкове». Ні — якщо ключа немає, воно впаде. Для необовʼязкового поля потрібен саме decodeIfPresent.",
            "Автосинтез робить це правильно сам: для Optional-властивості він генерує decodeIfPresent, тому в моделі з promoCode: String? відсутній ключ дає nil. Як тільки ти пишеш init(from:) руками, цю поведінку треба відтворити самому.",
          ],
          code: [
            "init(from decoder: Decoder) throws {",
            "    let c = try decoder.container(keyedBy: CodingKeys.self)",
            "    id = try c.decode(String.self, forKey: .id)   // ключ обовʼязковий",
            "    promoCode = try c.decodeIfPresent(String.self, forKey: .promoCode)",
            "    // try c.decode(String?.self, forKey: .promoCode) → keyNotFound без ключа",
            "}",
          ],
        },
        {
          q: "Як ти дебажиш DecodingError з продакшну — що в codingPath?",
          a: [
            "codingPath — шлях від кореня JSON до місця падіння: послідовність CodingKey, де для обʼєктів це імена ключів, а для масивів — індекси. Тобто items → 3 → price читається як «четвертий елемент items, поле price». Це головна річ, яку треба донести в лог.",
            "Тому в catch я не пишу localizedDescription, а розбираю DecodingError по чотирьох кейсах: typeMismatch (прийшов рядок замість числа), valueNotFound (null там, де тип не Optional), keyNotFound (ключ перейменували), dataCorrupted — найчастіше формат дати. Кожен лог — шлях плюс очікуваний тип.",
            "Далі відтворюю локально: беру сире тіло відповіді з логу і згодовую декодеру в тесті. Тому в дебаг-збірці мережевий шар мусить зберігати тіло — інакше шлях у тебе є, а даних, які його зламали, немає.",
          ],
          code: [
            "do {",
            "    return try decoder.decode(OrdersResponse.self, from: data)",
            "} catch let error as DecodingError {",
            "    switch error {",
            "    case .typeMismatch(let type, let ctx):",
            "        log(\"typeMismatch \\(type) at \\(ctx.codingPath.map(\\.stringValue))\")",
            "    case .keyNotFound(let key, let ctx):",
            "        log(\"keyNotFound \\(key.stringValue) at \\(ctx.codingPath)\")",
            "    default:",
            "        log(\"decoding failed: \\(error)\")",
            "    }",
            "    throw APIError.decoding",
            "}",
          ],
        },
      ],
    },
    {
      p: "P2", name: "Init, access control, KeyPath, type erasure",
      also: ["swift-19"],
      what: [
        "Ініціалізатори класів: designated (повна ініціалізація) і convenience (делегує designated). Two-phase init: фаза 1 — усі stored properties отримали значення знизу вгору по ієрархії; фаза 2 — можна чіпати self. failable init? повертає nil при невалідних даних. required — усі нащадки зобовʼязані реалізувати.",
        "Access control: private (тип+extensions у файлі) < fileprivate (файл) < internal (модуль, дефолт) < public (видно ззовні) < open (ще й можна успадковувати/override ззовні). private(set) — читати можна всім, писати лише всередині.",
        "KeyPath \\User.name — типізоване посилання на властивість як значення. Type erasure — приховування конкретного типу за обгорткою (AnyPublisher, AnyView): коли протокол з associated type не можна використати як тип напряму.",
      ],
      problem: "Two-phase init гарантує, що обʼєкт не використовується напівзібраним (звертання до self до ініціалізації всіх полів — помилка компіляції). Access control — інкапсуляція: private(set) var state дає читання ззовні, а мутацію лише власнику — менше місць, де стан може зламатись. Type erasure вирішує «protocol ... can only be used as a generic constraint».",
      where: [
        "init?(rawValue:) в enum; init? для валідації (User з порожнім імʼям — nil).",
        "private(set) у ViewModel: private(set) var orders: [Order] — View читає, змінює лише VM.",
        "KeyPath у API колекцій: map(\\.id), sorted(by: \\.createdAt) — і в SwiftUI (ForEach(items, id: \\.id)).",
        "open vs public — актуально для бібліотек/SDK: public клас ззовні НЕ можна сабкласити, open — можна.",
      ],
      code: [
        "final class OrderViewModel {",
        "    private(set) var orders: [Order] = []     // ззовні read-only",
        "    private let service: OrderService",
        "    init(service: OrderService) { self.service = service }  // DI через init",
        "}",
        "let names = users.map(\\.name)                 // KeyPath",
        "let adults = users.filter { $0.age >= 18 }",
      ],
      traps: [
        "У фазі 1 init класу не можна викликати методи self чи читати властивості — «self used before super.init».",
        "convenience init не може ініціалізувати stored properties напряму — тільки делегувати.",
        "private у Swift дозволяє доступ з extension того самого типу В ТОМУ Ж файлі — мʼякший за «private» в інших мовах.",
      ],
      qs: [
        {
          q: "Чим open відрізняється від public — і навіщо ця різниця бібліотекам?",
          a: [
            "Обидва видно з іншого модуля, але public клас ззовні не можна ні сабкласити, ні перевизначити його методи. open дозволяє і те, і те. Всередині свого модуля public успадковується вільно — різниця виникає рівно на межі модуля.",
            "Бібліотекам це потрібно тому, що успадкування — частина публічного контракту. Якщо клас open, автор зобовʼязаний тримати сумісність поведінки при override і не може вільно перебудувати внутрішні виклики. public без open дає видимість, не давши цієї обіцянки.",
            "Побічний бонус — оптимізація: public клас за межами модуля сабкласити нікому, тому компілятор може девіртуалізувати виклики; з open так не вийде. І open застосовний лише до класів та їхніх членів — для struct і enum його просто немає.",
          ],
        },
        {
          q: "Що таке two-phase initialization і які помилки він ловить?",
          a: [
            "Фаза 1: усі stored properties отримують значення, знизу вгору по ієрархії — спершу свої поля, потім super.init(). Фаза 2: після super.init() дозволено чіпати self — викликати методи, читати властивості, дописувати успадковані. Компілятор перевіряє цей порядок статично.",
            "Що він ловить: звертання до self до повної ініціалізації («self used before super.init»), забутий super.init() і поле, яке після init лишилось без значення. Тобто ситуація «метод прочитав поле, якого ще немає» — не баг у рантаймі, а помилка збірки.",
            "Деталь, яку варто згадати: саме порядок фази 1 робить Swift безпечнішим за Java чи C++ — нащадок заповнює свої поля ДО super.init(), тому перевизначений метод, викликаний з init базового класу, не побачить незаповнених полів. І convenience init писати в поля не може, лише делегувати.",
          ],
          code: [
            "class Base {",
            "    let id: String",
            "    init(id: String) { self.id = id }",
            "}",
            "final class Child: Base {",
            "    let title: String",
            "    init(id: String, title: String) {",
            "        self.title = title    // фаза 1: спершу свої поля",
            "        super.init(id: id)    // потім super",
            "        print(self.id)        // фаза 2: self уже доступний",
            "    }",
            "}",
          ],
        },
        {
          q: "Що таке type erasure, навіщо AnyView/AnyPublisher? Яку проблему з associated types це обходить?",
          a: [
            "Type erasure — сховати конкретний тип за обгорткою з фіксованим інтерфейсом. Обгортка тримає обʼєкт або замикання і перенаправляє на нього виклики. Ззовні тип один — AnyPublisher<Order, Error> — а що саме всередині, більше не видно.",
            "Проблема, яку це обходить: протокол з associated type не є повноцінним типом. У Publisher є Output і Failure, тому «якийсь Publisher» не повернеш із функції й не складеш різні паблішери в один масив — типи різні. eraseToAnyPublisher() зводить їх до одного і стабілізує сигнатуру API.",
            "Ціна — динамічний dispatch і втрачена інформація про тип. Тому AnyView у SwiftUI радять уникати: він ховає структуру body, фреймворк втрачає ідентичність вʼюшки і перестворює її замість оновлення. Де тип фактично один — краще some View; any і some частину випадків закрили без ручних обгорток.",
          ],
          code: [
            "struct AnyShape: Shape {",
            "    private let build: (CGRect) -> Path",
            "    init<S: Shape>(_ shape: S) { build = shape.path(in:) }",
            "    func path(in rect: CGRect) -> Path { build(rect) }",
            "}",
            "",
            "// Combine: різні типи паблішерів → один тип у сигнатурі",
            "func orders() -> AnyPublisher<[Order], Error> {",
            "    session.dataTaskPublisher(for: request)",
            "        .map(\\.data)",
            "        .decode(type: [Order].self, decoder: JSONDecoder())",
            "        .eraseToAnyPublisher()",
            "}",
          ],
        },
        {
          q: "Навіщо KeyPath, якщо є замикання — що він дає такого, чого closure не дасть?",
          a: [
            "KeyPath — це значення, яке ОПИСУЄ шлях до властивості, а не код, що її читає. Його можна порівнювати, зберігати як ключ у словнику, передавати як конфігурацію і використовувати в обидві сторони: читати і писати через WritableKeyPath.",
            "Звідси речі, які на closure не зробиш: users.map(\\.id), KeyPathComparator(\\.price) для сортування, переданого як дані, або таблиця «поле → валідатор», де ключем є сам шлях.",
            "Closure — чорна скринька: викликати можна, але не можна спитати, до чого саме вона зверталась, і не можна нею писати у властивість.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Типи даних углиб: struct, class, enum, tuple",
      also: ["swift-0", "swift-16"],
      what: [
        "Struct: отримує memberwise init безкоштовно (Point(x: 1, y: 2) безписання init руками — але будь-який кастомний init в тілі структури його вимикає). Методи, що змінюють властивості, позначаються mutating — під капотом вони отримують новий self (метод неявно приймає inout self). Свій Copy-on-Write будується через клас-буфер усередині структури + isKnownUniquelyReferenced(&buffer): якщо буфер тримає хтось ще — копіюємо перед мутацією.",
        "Class: єдиний тип з успадкуванням і deinit. Ідентичність перевіряється оператором === (той самий обʼєкт у памʼяті), а рівність — == (той самий вміст, вимагає Equatable). Два різні обʼєкти можуть бути == але не ===. required init — зобовʼязує сабкласи; convenience init — делегує до designated.",
        "Tuple: легка анонімна група значень — (min: 3, max: 10). Це value-тип без імені: не може мати методів, не підкоряється протоколам (не буде Codable/Equatable у своєму API), не може бути рекурсивним. Правило: tuple — для локального повернення двох-трьох значень з функції; щойно тип перетинає межу файлу/шару або потребує логіки — перетворюй на struct.",
        "Nested types — типи всередині типів для простору імен: Order.Status краще за OrderStatus (звʼязок видно з імені). typealias — синонім типу: typealias Completion = (Result<Order, APIError>) -> Void прибирає шум у сигнатурах. Any — будь-який тип узагалі (включно зі struct і функціями), AnyObject — лише class-типи (тому протоколи делегатів позначають AnyObject, щоб можна було weak).",
      ],
      problem: "Ця тема вирішує проблему вибору інструмента: коли struct, коли class, коли вистачить tuple. Неправильний вибір коштує дорого пізніше: моделі на класах дають несподівані спільні мутації, tuple-и, що розповзлись по шарах — неможливість додати метод чи conformance без глобального рефакторингу, а Any у публічних API — втрату типобезпеки і касти в рантаймі.",
      where: [
        "Свій COW — питання на співбесідах рівня middle+: «як зробити великий value-тип дешевим у копіюванні» — показуєш буфер-клас + isKnownUniquelyReferenced.",
        "=== використовується у кастомному Equatable для класів (спершу швидка перевірка ідентичності, потім полів) і в тестах — перевірити, що це САМЕ той інстанс (наприклад, синглтон або той самий делегат).",
        "Tuple щодня: повернення пари з функції — func minMax(_ a: [Int]) -> (min: Int, max: Int), координати, результат enumerated() у циклі.",
        "Nested types у моделях: Order.Status, Order.Payment — типовий стиль чистих доменних моделей; у твоєму проєкті так природно лягають статуси замовлення.",
        "AnyObject — обмеження протоколу делегата (protocol CartDelegate: AnyObject), без нього weak var delegate не скомпілюється.",
      ],
      code: [
        "// COW своїми руками:",
        "final class Buffer { var data: [Int]; init(_ d: [Int] = []) { data = d } }",
        "struct BigValue {",
        "    private var buffer = Buffer()",
        "    var data: [Int] { buffer.data }",
        "    mutating func append(_ x: Int) {",
        "        if !isKnownUniquelyReferenced(&buffer) {",
        "            buffer = Buffer(buffer.data) // копія лише коли буфер спільний",
        "        }",
        "        buffer.data.append(x)",
        "    }",
        "}",
        "",
        "// Ідентичність vs рівність:",
        "class User: Equatable {",
        "    let id: String; init(id: String) { self.id = id }",
        "    static func == (l: User, r: User) -> Bool { l === r || l.id == r.id }",
        "}",
        "",
        "// Tuple: добре локально…",
        "func minMax(_ a: [Int]) -> (min: Int, max: Int)? {",
        "    guard let f = a.first else { return nil }",
        "    return a.dropFirst().reduce((f, f)) { (Swift.min($0.0, $1), Swift.max($0.1, $1)) }",
        "}",
      ],
      traps: [
        "Кастомний init у ТІЛІ структури вимикає memberwise init; той самий init у extension — зберігає обидва. Тому кастомні ініціалізатори структур пишуть в extension.",
        "mutating-метод на let-структурі — помилка компіляції; властивість-структура всередині класу, оголошена let — теж незмінна, навіть якщо клас var.",
        "Tuple з іменами і без — сумісні за позицією: (min: 3, max: 5) можна присвоїти у (Int, Int) — імена загубляться мовчки.",
        "Порівняння tuple-ів працює лише до 6 елементів (обмеження стандартної бібліотеки) і лише якщо всі елементи Comparable/Equatable.",
        "Any ховає навіть Optional: масив [Any] може містити nil як Optional<T>.none — і as? розгортання дає сюрпризи. Побачив Any у своєму API — подумай ще раз.",
        "deinit не викликається детерміновано при крешах/exit(0) — не клади туди критичне збереження даних.",
      ],
      qs: [
        {
          q: "Коли зникає memberwise init і як зберегти його разом із кастомним?",
          a: [
            "Memberwise init генерується автоматично, поки в ТІЛІ структури немає жодного власного init. Щойно ти пишеш init у тілі — компілятор перестає його генерувати, і Point(x:y:) більше не існує.",
            "Рішення — виносити кастомні init в extension. Тіло структури лишається без init, memberwise генерується, а твій ініціалізатор додається поруч. Саме тому кастомні init структур за конвенцією пишуть в extension.",
            "Деталь: memberwise init має рівень internal, тому за межами модуля він недоступний — для публічного типу в бібліотеці public init усе одно пишеш руками.",
          ],
          code: [
            "struct Point {",
            "    var x: Double",
            "    var y: Double",
            "}",
            "",
            "extension Point {",
            "    init(both v: Double) { self.init(x: v, y: v) }",
            "}",
            "",
            "let a = Point(x: 1, y: 2)   // memberwise живий",
            "let b = Point(both: 3)      // і кастомний теж",
          ],
        },
        {
          q: "Напиши COW-обгортку: що робить isKnownUniquelyReferenced і чому параметр inout?",
          a: [
            "Дані тримаєш у класі-буфері всередині структури. Перед кожною мутацією питаєш isKnownUniquelyReferenced(&buffer): якщо власник один — мутуєш на місці, якщо ні — спершу дублюєш буфер. Так копія коштує O(n) лише коли її справді хтось бачить.",
            "inout потрібен, щоб функція працювала з самим сховищем посилання і не створювала ще одне тимчасове сильне посилання. Інакше під час перевірки лічильник завжди був би щонайменше два, і копія робилась би на кожну мутацію.",
            "Пастка: буфер мусить бути private — якщо посилання витекло назовні, унікальність зламана. І перевірка працює лише для нативних Swift-класів, не для NSObject-успадкованих.",
          ],
          code: [
            "final class Buffer {",
            "    var data: [Int]",
            "    init(_ data: [Int] = []) { self.data = data }",
            "}",
            "",
            "struct BigValue {",
            "    private var buffer = Buffer()",
            "    var data: [Int] { buffer.data }",
            "",
            "    mutating func append(_ x: Int) {",
            "        if !isKnownUniquelyReferenced(&buffer) {",
            "            buffer = Buffer(buffer.data)   // копія лише коли буфер спільний",
            "        }",
            "        buffer.data.append(x)",
            "    }",
            "}",
          ],
        },
        {
          q: "=== vs == — різниця? Чи можуть два обʼєкти бути == але не ===?",
          a: [
            "=== порівнює identity: чи це фізично той самий обʼєкт у памʼяті. == порівнює вміст і вимагає Equatable. === існує лише для reference-типів — для двох копій struct питання «той самий?» не має сенсу і навіть не компілюється.",
            "Так, можуть. Два різні User з однаковим id будуть ==, бо твій == дивиться на id, але === дасть false — це два окремі обʼєкти. Навпаки не буває: якщо === true, це один обʼєкт, і коректний == теж дасть true.",
            "Практика: у кастомному == для класу l === r ставлять першою перевіркою — швидкий shortcut, який відразу закриває найчастіший випадок.",
          ],
          code: [
            "final class User: Equatable {",
            "    let id: String",
            "    init(id: String) { self.id = id }",
            "    static func == (l: User, r: User) -> Bool { l === r || l.id == r.id }",
            "}",
            "",
            "let u1 = User(id: \"42\")",
            "let u2 = User(id: \"42\")",
            "print(u1 == u2)    // true  — однаковий вміст",
            "print(u1 === u2)   // false — різні обʼєкти",
          ],
        },
        {
          q: "Коли tuple стає боргом і його час міняти на struct? Назви три обмеження tuple.",
          a: [
            "Tuple добрий, поки живе локально: повернув пару з функції — одразу розпакував. Борг починається, коли він перетинає межу шару: зʼявляється у публічній сигнатурі, кладеться в масив, їде між модулями. Захотілося дати йому typealias — це вже сигнал писати struct.",
            "Обмеження: не може підкорятися протоколам, тому не буде ні Codable, ні Equatable; не може мати методів і computed properties; не може бути рекурсивним. Плюс імена елементів не частина типу — (min: 3, max: 5) мовчки присвоюється у (Int, Int) і імена губляться.",
            "Ще деталь: вбудоване порівняння tuple-ів працює лише до шести елементів і лише коли всі елементи Comparable.",
          ],
        },
        {
          q: "Any vs AnyObject — різниця, і чому протокол делегата має бути AnyObject?",
          a: [
            "Any — будь-який тип узагалі: struct, enum, замикання, class. AnyObject — лише class-типи. Тому AnyObject можна тримати у weak, а Any — ні.",
            "Делегата зберігають як weak var, щоб не було retain cycle між VC і його дочірнім обʼєктом. weak можливий тільки для reference-типів — ARC має що обнулити. Без protocol CartDelegate: AnyObject компілятор просто не дасть написати weak var delegate.",
            "Пастка з Any: він ховає навіть Optional — у масиві [Any] nil лежить як Optional<T>.none, і as? розгортання дає сюрпризи. Побачив Any у власному API — швидше за все там просто бракує generic-а.",
          ],
          code: [
            "protocol CartDelegate: AnyObject {",
            "    func cartDidChange(itemsCount: Int)",
            "}",
            "",
            "final class Cart {",
            "    weak var delegate: CartDelegate?   // без AnyObject — не скомпілюється",
            "}",
          ],
        },
      ],
    },
    {
      p: "P1", name: "ООП: інкапсуляція, успадкування, поліморфізм, абстракція",
      what: [
        "Обʼєктно-орієнтоване програмування у Swift базується на принципах інкапсуляції, успадкування, поліморфізму та абстракції.",
      ],
      sections: [
        {
          title: "1. Інкапсуляція",
          text: "Обʼєднання даних (властивостей) і методів в одному типі, а також обмеження доступу до деяких компонентів обʼєкта. Приховування даних і доступ через методи або властивості. Суть: ми не даємо прямого доступу до balance, а керуємо через методи. Рівні доступу в Swift: private, fileprivate, internal (дефолт), public, open; private(set) — читати можна всім, писати лише всередині типу.",
          code: [
            "final class BankAccount {",
            "    private var balance: Decimal = 0          // прямого доступу ззовні немає",
            "    var currentBalance: Decimal { balance }   // читати можна",
            "    func deposit(_ amount: Decimal) { balance += amount }",
            "    func withdraw(_ amount: Decimal) -> Bool {",
            "        guard amount <= balance else { return false }",
            "        balance -= amount; return true",
            "    }",
            "}",
          ],
        },
        {
          title: "2. Успадкування",
          text: "Створення класів на основі інших класів. Успадкування дозволяє одному класу успадковувати властивості та методи іншого — це сприяє повторному використанню коду. У Swift успадкування підтримують лише class (не struct і не enum). Нащадок викликає super.init(...) після ініціалізації своїх полів.",
          code: [
            "class Vehicle {",
            "    var speed: Double",
            "    init(speed: Double) { self.speed = speed }",
            "    func drive() {",
            "        print(\"Їду зі швидкістю \\(speed) км/год\")",
            "    }",
            "}",
            "class Bike: Vehicle {",
            "    var type: String",
            "    init(speed: Double, type: String) {",
            "        self.type = type",
            "        super.init(speed: speed)",
            "    }",
            "    func displayBikeInfo() {",
            "        print(\"Тип велосипеда: \\(type), Швидкість: \\(speed) км/год\")",
            "    }",
            "}",
            "let myBike = Bike(speed: 15, type: \"Гірський\")",
            "myBike.displayBikeInfo()",
            "// Виведе: Тип велосипеда: Гірський, Швидкість: 15.0 км/год",
          ],
        },
        {
          title: "3. Поліморфізм",
          text: "Один і той самий метод працює по-різному для різних обʼєктів. У Swift це зазвичай реалізується через успадкування (override методів) або через протоколи. Масив [Animal] з Dog і Cat — у циклі animal.sound() викликає правильну реалізацію кожного типу.",
          code: [
            "class Animal {",
            "    func sound() { print(\"Тварина видає звук\") }",
            "}",
            "class Dog: Animal {",
            "    override func sound() { print(\"Гав!\") }",
            "}",
            "class Cat: Animal {",
            "    override func sound() { print(\"Мяу!\") }",
            "}",
            "let dog = Dog()",
            "let cat = Cat()",
            "let animals: [Animal] = [dog, cat]",
            "for animal in animals { animal.sound() }",
            "// Виведе:",
            "// Гав!",
            "// Мяу!",
          ],
        },
        {
          title: "4. Абстракція",
          text: "Протоколи (Protocols) визначають інтерфейс без обовʼязкової реалізації. Абстракція — виділення головних характеристик та поведінки обʼєкта, приховуючи деталі реалізації. Мета: працювати з «що робить обʼєкт», а не «як він це робить». У Swift — через протоколи або базові класи, які не використовуються напряму, а лише наслідуються.",
          code: [
            "protocol PaymentMethod {",
            "    func pay(amount: Decimal)",
            "}",
            "struct ApplePay: PaymentMethod {",
            "    func pay(amount: Decimal) { print(\"Оплата \\(amount) через Apple Pay\") }",
            "}",
            "struct Cash: PaymentMethod {",
            "    func pay(amount: Decimal) { print(\"Оплата \\(amount) готівкою\") }",
            "}",
            "func checkout(methods: [any PaymentMethod], total: Decimal) {",
            "    methods.forEach { $0.pay(amount: total) }  // не важливо ЯК — важливо ЩО",
            "}",
          ],
        },
      ],
      problem: "Без інкапсуляції будь-хто може зламати інваріанти обʼєкта (наприклад, встановити відʼємний balance). Без успадкування довелось би копіювати однаковий код у кожен клас. Без поліморфізму — писати if type(of: x) is Dog для кожної операції замість універсального циклу. Без абстракції код жорстко привʼязаний до конкретної реалізації і його важко тестувати чи замінювати.",
      where: [
        "Інкапсуляція: BankAccount з private var balance; ViewModel з private(set) var state — UI читає, змінює лише власник.",
        "Успадкування: UIViewController → BaseViewController зі спільною логікою; UITableViewCell → кастомна комірка. У доменних моделях Apple радить struct + протоколи замість глибоких ієрархій class.",
        "Поліморфізм: [UIView] з різними підкласами; override layoutSubviews; масив [Animal] з Dog і Cat; або [any PaymentMethod] з різними способами оплати.",
        "Абстракція: protocol OrderService — ViewModel не знає, API це чи Mock; UITableViewDataSource — UITableView не знає, який саме екран його реалізує.",
      ],
      traps: [
        "У Swift struct і enum не успадковуються — лише class. Conformance до протоколу — це не успадкування.",
        "override обовʼязковий у нащадка; без нього — помилка компіляції (або приховання методу батька).",
        "Глибокі ієрархії class (3+ рівні) — антипатерн; Swift на практиці віддає перевагу композиції та протоколам (POP).",
        "«Абстрактний клас» у Swift — не окреме ключове слово: базовий class, який не інстанціюють напряму, або протокол.",
        "Поліморфізм через протокол без оголошення методу в тілі протоколу (лише в extension) — static dispatch; див. тему «Protocols і POP».",
      ],
      qs: [
        {
          q: "Назви чотири принципи ООП і поясни кожен на прикладі Swift.",
          a: [
            "Інкапсуляція — дані й методи в одному типі плюс закритий доступ: private var balance, а зміни лише через deposit/withdraw. Успадкування — клас на основі іншого: Bike: Vehicle отримує speed і drive() і додає своє.",
            "Поліморфізм — один виклик, різні реалізації: у масиві [Animal] з Dog і Cat виклик animal.sound() дає «Гав!» і «Мяу!». Абстракція — працюєш із «що робить обʼєкт», а не «як»: protocol PaymentMethod, а ApplePay чи Cash — вже деталь реалізації.",
            "Специфіка Swift: успадкування є лише у class. Решту три принципи однаково отримують struct і enum — через access control, протоколи і protocol extensions.",
          ],
        },
        {
          q: "Інкапсуляція: чому balance — private, а не public var?",
          a: [
            "Бо з public var будь-хто може написати account.balance = -500 і зламати інваріант типу. private var + методи deposit/withdraw роблять так, що баланс змінюється лише перевіреними шляхами.",
            "Правило «не можна зняти більше, ніж є» живе в одному місці — у withdraw, який повертає false. З відкритою властивістю ця перевірка розповзлася б по всіх викликачах, і хтось її обовʼязково забув би. Читання лишається доступним через computed property currentBalance.",
            "Коротший варіант того самого — private(set) var balance: читати всім, писати лише всередині типу.",
          ],
          code: [
            "final class BankAccount {",
            "    private var balance: Decimal = 0",
            "    var currentBalance: Decimal { balance }",
            "",
            "    func deposit(_ amount: Decimal) { balance += amount }",
            "    func withdraw(_ amount: Decimal) -> Bool {",
            "        guard amount <= balance else { return false }",
            "        balance -= amount",
            "        return true",
            "    }",
            "}",
          ],
        },
        {
          q: "Успадкування: напиши Vehicle → Bike з super.init. Навіщо super після своїх полів?",
          a: [
            "Bike спершу присвоює свій type, і лише потім кличе super.init(speed:). Це two-phase initialization: до виклику super всі власні stored properties мусять мати значення, інакше компілятор не пропустить.",
            "Причина — після super.init обʼєкт вважається повністю ініціалізованим, і саме з цієї миті можна торкатися self, викликати методи й override-и. Якби super.init стартував раніше, батьківський init міг би викликати override-нутий метод, який читає ще не ініціалізоване поле нащадка.",
            "Друга фаза — після super.init: там уже можна доналаштовувати self і перезаписувати успадковані властивості.",
          ],
          code: [
            "class Vehicle {",
            "    var speed: Double",
            "    init(speed: Double) { self.speed = speed }",
            "    func drive() { print(\"Їду зі швидкістю \\(speed) км/год\") }",
            "}",
            "",
            "class Bike: Vehicle {",
            "    var type: String",
            "    init(speed: Double, type: String) {",
            "        self.type = type          // 1) свої поля",
            "        super.init(speed: speed)  // 2) потім батько",
            "    }",
            "}",
          ],
        },
        {
          q: "Поліморфізм: що виведе цикл for animal in animals { animal.sound() }?",
          a: [
            "«Гав!» і «Мяу!» — по рядку на елемент. Статичний тип елемента масиву — Animal, але викликається реалізація фактичного типу обʼєкта.",
            "Механіка: sound() у класі йде через vtable, тобто dynamic dispatch — потрібна реалізація вибирається у рантаймі за реальним типом. Саме тому і працює один універсальний цикл замість перевірок if x is Dog.",
            "Пастка поруч: якби sound() існував лише в extension протоколу без оголошення в тілі протоколу, dispatch став би статичним і вивелось би базове двічі. А забути override у класі не вийде — це помилка компіляції.",
          ],
        },
        {
          q: "Абстракція: чим protocol PaymentMethod відрізняється від struct ApplePay?",
          a: [
            "Protocol — це контракт: «є метод pay(amount:)», без жодної реалізації. ApplePay — конкретне виконання цього контракту, тобто відповідь на «як саме списати гроші».",
            "Наслідок: checkout(methods: [any PaymentMethod], total:) написаний один раз і не змінюється, коли додається Cash чи новий провайдер. У тестах підставляєш MockPayment і перевіряєш логіку оформлення без реального платежу.",
            "Деталь: протокол не можна інстанціювати — PaymentMethod() не існує. any PaymentMethod — це вже існуючий обʼєкт, схований за контрактом; сам контракт нічого не вміє робити.",
          ],
        },
        {
          q: "Коли краще протокол, а коли успадкування class?",
          a: [
            "Протокол — за замовчуванням. Він дає абстракцію без ієрархії, працює для struct і enum, один тип може підкорятися кільком протоколам, а спільну реалізацію роздають через protocol extension. Ще й підміняється в тестах без сабкласів.",
            "Class-успадкування беру там, де його вимагає фреймворк: свій екран — це UIViewController, комірка — UITableViewCell, і без override viewDidLoad нічого не працює. Або коли нащадкам треба реально ділити stored properties, чого протокол не дає.",
            "Пастка: ієрархія на три й більше рівні — сигнал, що потрібна композиція. У доменних моделях глибокі class-ієрархії з UIKit копіювати не варто.",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Classes: відмінності від struct, успадкування, deinit",
      also: ["swift-0", "swift-14"],
      what: [
        "На перший погляд class схожий на struct: і той і той дозволяє створити новий тип з властивостями та методами. Але class додає успадкування — можливість будувати один клас на основі іншого. Це потужна фіча, і в реальних iOS-додатках (UIKit, AppKit) без класів не обійтись. Але тримай код простим: наявність фічі не означає, що її треба використовувати. Як писав Martin Fowler: «будь-хто може написати код, зрозумілий компʼютеру; хороші програмісти пишуть код, зрозумілий людям».",
        "Джерело структури теми: [Hacking with Swift — Day 10: Classes](https://www.hackingwithswift.com/100/10). Нижче — пʼять ключових відмінностей class від struct, кожна з прикладом коду.",
      ],
      sections: [
        {
          title: "1. Створення class — немає memberwise init",
          text: "На відміну від struct, class ніколи не отримує безкоштовний memberwise initializer. Якщо у class є stored properties без дефолтного значення — потрібен свій init. Три варіанти: зробити поля optional (зазвичай погано), дати дефолтні значення, або написати init вручну — це правильний шлях. init — спеціальний метод без ключового слова func; self. розрізняє параметр і властивість.",
          code: [
            "class Dog {",
            "    var name: String",
            "    var breed: String",
            "",
            "    init(name: String, breed: String) {",
            "        self.name = name",
            "        self.breed = breed",
            "    }",
            "}",
            "let poppy = Dog(name: \"Poppy\", breed: \"Poodle\")",
          ],
        },
        {
          title: "2. Class inheritance — успадкування",
          text: "Class може будуватись на іншому class: CountrySinger: Singer. Двокрапка означає «extends». Нащадок (subclass) отримує властивості й методи батька (superclass) і додає свої. У UIKit/AppKit ієрархії класів — скрізь: UIView → UILabel, UIViewController → твій екран. Не копіюй код батька вручну — зміни в батьківському класі інакше не потраплять у нащадків.",
          code: [
            "class Singer {",
            "    var name: String",
            "    init(name: String) { self.name = name }",
            "    func sing() { print(\"La la la\") }",
            "}",
            "class CountrySinger: Singer {",
            "    override func sing() {",
            "        print(\"Trucks, guitars, and liquor\")",
            "    }",
            "}",
            "let taylor = CountrySinger(name: \"Taylor\")",
            "taylor.sing()  // Trucks, guitars, and liquor",
          ],
        },
        {
          title: "3. Overriding methods — override",
          text: "Щоб змінити метод, успадкований від батька, потрібно override func. Ключове слово override робить намір явним і дозволяє компілятору перевірити: без override — не скомпілюється; override без методу в батьку — теж помилка. Якщо нащадок додає власні stored properties, init нащадка спочатку ініціалізує свої поля, потім викликає super.init(...) — передає параметри батьківському класу.",
          code: [
            "class HeavyMetalSinger: Singer {",
            "    var noiseLevel: Int",
            "    init(name: String, noiseLevel: Int) {",
            "        self.noiseLevel = noiseLevel",
            "        super.init(name: name)       // після своїх полів!",
            "    }",
            "    override func sing() {",
            "        print(\"Grrrrr rargh!\")",
            "    }",
            "}",
          ],
        },
        {
          title: "4. Final classes — заборона успадкування",
          text: "final class забороняє створювати підкласи. Це явний намір («цей клас не для розширення») і оптимізація: компілятор може застосувати static dispatch замість dynamic. Оголошуй final класи, які не плануєш наслідувати: ViewModel, сервіси, утиліти. У великих кодових базах final — дефолт, open — лише для SDK і бібліотек.",
          code: [
            "final class OrderViewModel {",
            "    private(set) var orders: [Order] = []",
            "    // ніхто не зможе зробити class EvilVM: OrderViewModel",
            "}",
          ],
        },
        {
          title: "5. Copying objects — reference semantics",
          text: "При копіюванні struct отримуєш незалежну копію (value type). При «копіюванні» class обидві змінні вказують на той самий обʼєкт у heap (reference type): зміна через одну — видна через іншу. Class дає гнучкість (спільний стан), struct — безпеку (ізольовані копії). UIKit: UI-елементи — class (ідентичність на екрані), моделі даних — struct.",
          code: [
            "class Counter { var value = 0 }",
            "let a = Counter()",
            "let b = a              // не копія обʼєкта — те саме посилання",
            "b.value = 10",
            "print(a.value)         // 10",
            "",
            "struct Point { var x = 0 }",
            "var p1 = Point(); var p2 = p1",
            "p2.x = 5",
            "print(p1.x)            // 0 — незалежна копія",
          ],
        },
        {
          title: "6. Deinitializers — deinit",
          text: "Лише class має deinit — метод, що викликається, коли останнє strong-посилання зникає (ARC → 0). Struct не має deinit — value-типи зникають разом із областю видимості. deinit — місце для cleanup: invalidate таймерів, зняття observers, логування для дебагу ліків. Не покладайся на deinit для критичного збереження даних при креші.",
          code: [
            "class Screen {",
            "    var timer: Timer?",
            "    deinit {",
            "        timer?.invalidate()",
            "        print(\"Screen deinit\")  // якщо не друкується — можливий retain cycle",
            "    }",
            "}",
          ],
        },
        {
          title: "7. Mutability — let class vs let struct",
          text: "let struct заморожує весь вміст — змінити var-поле неможливо. let class фіксує лише посилання: переприсвоїти інший обʼєкт не можна, але var-властивості всередині змінювати можна. Це одна з найчастіших пасток на співбесідах після value vs reference.",
          code: [
            "class Dog { var name = \"Rex\" }",
            "let dog = Dog()",
            "dog.name = \"Buddy\"     // OK — змінюємо вміст обʼєкта",
            "// dog = Dog()         // помилка — посилання незмінне",
            "",
            "struct Cat { var name = \"Mia\" }",
            "let cat = Cat()",
            "// cat.name = \"Leo\"   // помилка — let struct незмінний",
          ],
        },
      ],
      problem: "Class вирішує задачі, де потрібна ідентичність обʼєкта, спільний мутабельний стан, успадкування (UIKit) і детерміноване звільнення ресурсів через deinit. Без розуміння цих відмінностей від struct легко отримати несподівані спільні мутації, ліки памʼяті або помилки з init/super.init.",
      where: [
        "UIView, UIViewController, UITableViewCell — усе class з глибокими ієрархіями успадкування.",
        "ViewModel (ObservableObject), мережеві менеджери, кеші — class для спільного стану між частинами app.",
        "override — у кожному кастомному UIViewController (viewDidLoad), UITableViewCell, делегатах.",
        "final — ViewModel і сервіси, які не плануєш наслідувати.",
        "deinit — перевірка, що VC деалокується після pop (print/debug); invalidate таймерів і NotificationCenter observers.",
        "@objc / @objcMembers — коли UIKit/Objective-C runtime має викликати метод Swift-класу (селектори, #selector).",
      ],
      traps: [
        "Забув super.init у init нащадка — помилка компіляції або невизначена поведінка.",
        "override без ключового слова — не скомпілюється.",
        "Думаєш, що let class = незмінний обʼєкт — насправді незмінне лише посилання.",
        "Копіювання class-змінної — не копія обʼєкта; для справжньої копії потрібен NSCopying або власна логіка.",
        "Глибокі ієрархії (A → B → C → D) — важко підтримувати; не копіюй патерн UIKit у доменні моделі.",
      ],
      qs: [
        {
          q: "Назви пʼять відмінностей class від struct. Чому struct має memberwise init, а class — ні?",
          a: [
            "Перше — семантика копіювання: class передається за посиланням, struct копіює значення. Друге — успадкування є лише у class. Третє — deinit є лише у class. Четверте — let для класу фіксує посилання, а не вміст. Пʼяте — class не отримує memberwise init і живе в heap під ARC.",
            "Memberwise init безпечний для struct, бо набір полів фіксований: нащадків немає, додати поля ніхто не може. У класу є успадкування — згенерований init не знав би, які поля батька приймати і як викликати super.init, тому компілятор не вгадує і вимагає писати init самому.",
            "Практично це означає три варіанти для класу: дефолтні значення полів, Optional-поля (зазвичай погано) або власний init — і третій правильний.",
          ],
        },
        {
          q: "Що означає override і коли потрібен super.init?",
          a: [
            "override — явна заява «я замінюю член батька». Компілятор перевіряє обидві сторони: без override метод-двійник не скомпілюється, а override без відповідника в батьку — теж помилка. Це рятує від «я думав, що перекрив, а насправді додав новий метод».",
            "super.init потрібен, коли нащадок додає власні stored properties: спершу присвоюєш свої поля, потім super.init(...), який доініціалізує частину батька. До цього виклику self торкатися не можна.",
            "Практика: в override-методах UIKit майже завжди треба кликати super — пропущений super.viewDidLoad() або super.layoutSubviews() ламає поведінку фреймворку тихо, без помилки компіляції.",
          ],
        },
        {
          q: "Навіщо final class? Що дає компілятору?",
          a: [
            "final забороняє сабкласи й override. Насамперед це намір: «цей клас не точка розширення» — ViewModel, сервіс, утиліта. Читаючи код, ти знаєш, що поведінку не перевизначили десь у нащадку.",
            "Компілятору final прибирає невизначеність: якщо метод не можна override, виклик не потрібно шукати у vtable — його девіртуалізують до статичного і часто інлайнять. Той самий ефект дає final на окремому методі або private-метод.",
            "Деталь: у межах одного модуля з whole-module optimization компілятор часто виводить це сам, але final — гарантія і документація. Протилежність для бібліотек — open, тобто «наслідувати можна ззовні модуля».",
          ],
        },
        {
          q: "Що виведе код з двома посиланнями на один Counter?",
          a: [
            "10. b = a не копіює обʼєкт: обидві змінні тримають посилання на той самий Counter у heap, тому b.value = 10 видно і через a.value.",
            "Зверни увагу, що a оголошений через let — і це не перешкода. Незмінне саме посилання, а не вміст обʼєкта. Той самий код зі struct вивів би 0, бо друга змінна отримала б незалежну копію.",
            "Тут і різниця в одному реченні: value-тип копіює значення, reference-тип копіює посилання.",
          ],
          code: [
            "final class Counter { var value = 0 }",
            "let a = Counter()",
            "let b = a          // те саме посилання",
            "b.value = 10",
            "print(a.value)     // 10",
            "",
            "struct Point { var x = 0 }",
            "var p1 = Point()",
            "var p2 = p1        // незалежна копія",
            "p2.x = 5",
            "print(p1.x)        // 0",
          ],
        },
        {
          q: "Чому let dog.name = \"Buddy\" працює для class, але не для let struct?",
          a: [
            "let заморожує вміст самої змінної. У класу вміст — це посилання: dog = Dog() заборонено, а dog.name = \"Buddy\" змінює обʼєкт за посиланням, і посилання лишається тим самим.",
            "У структури вміст — це саме значення разом з усіма полями. Тому let заморожує все: ні присвоїти поле, ні викликати mutating-метод — обидва варіанти помилка компіляції.",
            "Пастка: властивість-структура, оголошена let усередині класу, теж незмінна — навіть якщо сам обʼєкт у var і решта полів мутабельні. Це одне з найчастіших «чому не компілюється».",
          ],
        },
        {
          q: "Коли викликається deinit і чому struct його не має?",
          a: [
            "Коли зникає останнє сильне посилання: ARC доводить лічильник до нуля і перед звільненням памʼяті кличе deinit. Параметрів він не має, викликати руками неможливо, і в нащадку після власного deinit автоматично виконується батьківський.",
            "У struct немає обʼєкта в heap, на який хтось посилається: копія зникає разом зі своєю областю видимості, моменту «останній власник відпустив» просто не існує — рахувати нічого. Тому deinit є лише у class (і в actor).",
            "Практика: у deinit — timer?.invalidate() і зняття observers, а print у deinit — найдешевший детектор retain cycle: не друкується після pop — щось тримає екран. Критичне збереження даних туди не клади: при креші чи exit deinit не гарантований.",
          ],
          code: [
            "final class Screen {",
            "    var timer: Timer?",
            "    deinit {",
            "        timer?.invalidate()",
            "        print(\"Screen deinit\")   // не друкується → шукай retain cycle",
            "    }",
            "}",
          ],
        },
        {
          q: "Class дає гнучкість чи безпеку порівняно з struct — і коли що обирати?",
          a: [
            "Гнучкість. Class дає спільний стан, ідентичність, успадкування і deinit: один обʼєкт бачать усі, хто на нього посилається. Безпеку дає struct — у кожного своя копія, ніхто ззовні її не змінить, немає ARC і немає ризику retain cycle.",
            "Тому обираю struct за замовчуванням: моделі, DTO, стани екрана. Class беру, коли потрібна ідентичність або спільний мутабельний стан — сесія, кошик, кеш, мережевий менеджер, ViewModel, і все успадковане від UIKit.",
            "Пастка: class замість struct у моделі дає найважчий клас багів — «мій обʼєкт змінився, а я цього не робив». Там, де class уже неминучий, підтягуй final і private(set), щоб звузити цю гнучкість назад.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Error Handling: throw, throws, try, catch",
      also: ["swift-9"],
      what: [
        "Error Handling — механізм Swift, який дозволяє безпечно повідомляти про помилки та обробляти їх, замість того щоб застосунок аварійно завершувався. Ключові слова: Error (протокол типу помилки) → throws (функція може кинути) → throw (кинути конкретну помилку) → try (виклик throwing-функції) → catch (обробити).",
        "Як запамʼятати: Error описує які помилки можуть існувати; throws каже, що функція може завершитися помилкою; throw створює та викидає конкретну помилку; try викликає таку функцію; catch ловить і обробляє. Головне для співбесіди: throw ≠ crash — він лише передає інформацію про помилку тому, хто викликав функцію.",
      ],
      sections: [
        {
          title: "1. Протокол Error і власні типи помилок",
          text: "Error — вбудований протокол Swift без методів і властивостей. Його єдина задача — повідомити компілятору: «цей тип можна використовувати як помилку». Найчастіше помилки — enum з кейсами (зручно для switch у catch). Можна також struct або class — головне conformance до Error.",
          code: [
            "// protocol Error { }  — порожній маркерний протокол",
            "",
            "enum LoginError: Error {",
            "    case wrongPassword",
            "    case userNotFound",
            "}",
            "",
            "struct NetworkError: Error {",
            "    let message: String",
            "}",
          ],
        },
        {
          title: "2. throw — викинути помилку",
          text: "throw — оператор, який зупиняє виконання функції і передає помилку вгору по стеку викликів. «Я не можу продовжити — повідомляю про проблему». Використовуємо, коли функція не може виконати задачу: неправильний пароль, файл не знайдено, немає доступу, помилка сервера, ділення на нуль. Не кожна незручна ситуація — помилка: якщо «користувача не знайдено» — очікуваний кейс, краще повернути nil (User?), а не throw.",
          code: [
            "enum AgeError: Error { case underAge }",
            "",
            "func enterClub(age: Int) throws {",
            "    if age < 18 { throw AgeError.underAge }",
            "    print(\"Welcome!\")",
            "}",
            "",
            "enum MathError: Error { case divideByZero }",
            "func divide(_ a: Int, by b: Int) throws -> Int {",
            "    if b == 0 { throw MathError.divideByZero }",
            "    return a / b",
            "}",
          ],
        },
        {
          title: "3. throws — функція може кинути помилку",
          text: "throws у сигнатурі означає: «ця функція може викинути помилку всередині». Компілятор знає про throw і змушує викликача явно обробити ризик — через try + do-catch, try?, try! або пробросити далі (throws у викликаючій функції). Без цього код не скомпілюється.",
          code: [
            "func login(password: String) throws { ... }",
            "func divide(_ a: Int, by b: Int) throws -> Int { ... }",
            "",
            "// Пробросити помилку далі — не ловити тут:",
            "func loadAndParse() throws -> Data {",
            "    let data = try Data(contentsOf: fileURL)  // throw піде вгору",
            "    return data",
            "}",
          ],
        },
        {
          title: "4. try і do-catch — обробити помилку",
          text: "Якщо функція має throws, викликаємо її через try. do-catch — основний спосіб обробки: спробувати виконати блок, при throw перейти в catch. У catch без типу — локальна змінна error містить викинуту помилку. Можна ловити конкретні кейси: catch LoginError.wrongPassword { ... }.",
          code: [
            "do {",
            "    let result = try divide(10, by: 2)",
            "    print(result)                    // 5",
            "} catch {",
            "    print(error)                     // будь-яка помилка",
            "}",
            "",
            "do {",
            "    try enterClub(age: 15)",
            "} catch AgeError.underAge {",
            "    print(\"Access denied\")",
            "} catch {",
            "    print(\"Інша помилка: \\(error)\")",
            "}",
          ],
        },
        {
          title: "5. try? і try! — без do-catch",
          text: "try? — якщо помилка, повертає nil замість crash. Тип результату стає Optional: try? divide(10, by: 0) → Int?. Зручно, коли nil — прийнятна відповідь, але причина помилки губиться — для продакшн-дебагу краще do/catch з логом. try! — «на 100% впевнений, що помилки не буде». Якщо помилка все ж виникне — crash (як force unwrap). Легітимно лише для ресурсів, які гарантовано є в бандлі.",
          code: [
            "let ok = try? divide(10, by: 2)      // Optional(5)",
            "let fail = try? divide(10, by: 0)  // nil",
            "",
            "let safe = try! divide(10, by: 2)    // 5 — OK",
            "// let crash = try! divide(10, by: 0) // 💥 runtime crash",
          ],
        },
        {
          title: "6. throw ≠ crash — обробка в UI",
          text: "Коли використовуєш throw + do-catch, застосунок не падає — ти сам вирішуєш у catch: показати Alert, повторити запит, записати в лог, повернути на попередній екран. Crash буде лише при try! на помилці. Swift змушує обробити throws на етапі компіляції — не можна викликати throwing-функцію «мовчки».",
          code: [
            "enum LoginError: Error {",
            "    case emptyPassword",
            "    case wrongPassword",
            "}",
            "func login(password: String) throws {",
            "    if password.isEmpty { throw LoginError.emptyPassword }",
            "    if password != \"1234\" { throw LoginError.wrongPassword }",
            "    print(\"Login successful\")",
            "}",
            "",
            "// Користувач натиснув «Увійти» → throw → catch → Alert → вводить знову",
            "do {",
            "    try login(password: userInput)",
            "} catch LoginError.wrongPassword {",
            "    showAlert(title: \"Помилка\", message: \"Неправильний пароль\")",
            "} catch {",
            "    showAlert(title: \"Помилка\", message: error.localizedDescription)",
            "}",
          ],
        },
        {
          title: "7. Повний приклад — кілька catch",
          text: "Один do може мати кілька catch-блоків — спочатку конкретні типи/кейси, загальний catch — останнім. Порядок важливий: від специфічного до загального.",
          code: [
            "do {",
            "    try login(password: \"1111\")",
            "} catch LoginError.emptyPassword {",
            "    print(\"Password is empty\")",
            "} catch LoginError.wrongPassword {",
            "    print(\"Wrong password\")",
            "} catch {",
            "    print(error)",
            "}",
            "// Виведе: Wrong password",
          ],
        },
        {
          title: "8. throw vs Optional — коли що",
          text: "throw — коли збій неочікуваний або критичний для поточної операції (мережа, парсинг, авторизація). Optional/nil — коли відсутність результату нормальна (пошук у словнику, findUser(id:) → User?). Result<T, Error> — коли потрібно зберегти успіх або помилку без throw (колбеки, до async/await).",
          code: [
            "// Очікувана відсутність — Optional:",
            "func findUser(id: Int) -> User? { ... }",
            "",
            "// Критична операція — throws:",
            "func fetchUser(id: Int) async throws -> User {",
            "  let (data, _) = try await URLSession.shared.data(from: url)",
            "  return try JSONDecoder().decode(User.self, from: data)",
            "}",
          ],
        },
      ],
      problem: "Без типізованого error handling помилки перетворюються на креші, мовчазні nil або неконтрольовані print. throws + Error дають: помилки в системі типів (компілятор змушує обробити), зрозумілі доменні кейси (LoginError.wrongPassword), і контрольовану реакцію в UI — без падіння застосунку.",
      where: [
        "Мережевий шар: enum APIError: Error { case unauthorized, server(code: Int), decoding } — централізована обробка (див. також тему Enums).",
        "async throws у URLSession, JSONDecoder.decode, FileManager — стандартна бібліотека повсюдно throwing.",
        "ViewModel: do/catch навколо try await fetchOrders(), у catch — оновити state на .failed(message).",
        "Логін/реєстрація: throw LoginError → catch → UIAlertController або SwiftUI .alert.",
        "Codable: try decoder.decode — DecodingError з codingPath для дебагу продакшн-парсингу.",
      ],
      traps: [
        "try? ковтає причину помилки — для дебагу краще do/catch з логуванням error.",
        "try! у продакшні — креш, який знайде користувач; як force unwrap для помилок.",
        "throw для очікуваних кейсів (user not found) — overkill; краще Optional або Result.",
        "Забути throws у викликаючій функції — не скомпілюється; це фіча, не баг.",
        "catch без загального блоку після специфічних — необроблені кейси enum можуть «просочитись» (exhaustiveness у switch, не в catch — але порядок catch важливий).",
        "Плутати throw (runtime) з fatalError/preconditionFailure (навмисний crash для програмістських багів).",
      ],
      qs: [
        {
          q: "Що таке протокол Error і навіщо він порожній?",
          a: [
            "Error — маркерний протокол без методів і властивостей. Його єдина роль — сказати компілятору, що тип можна кинути через throw і зловити в catch.",
            "Порожній він тому, що вимагати спільної поведінки нема сенсу: помилка мережі й помилка валідації нічого спільного не роблять. Найчастіше Error реалізують як enum — тоді у catch зручно матчити конкретні кейси; struct або class беруть, коли потрібні payload-поля, як код відповіді сервера.",
            "Деталь: якщо текст помилки йде користувачу, додай LocalizedError з errorDescription — інакше localizedDescription для власного enum дає технічний рядок, який на екрані показувати не можна.",
          ],
          code: [
            "enum LoginError: Error {",
            "    case wrongPassword",
            "    case userNotFound",
            "}",
            "",
            "struct ServerError: Error {",
            "    let code: Int",
            "    let message: String",
            "}",
          ],
        },
        {
          q: "Різниця throw, throws, try — по одному реченню на кожне.",
          a: [
            "throw — оператор, який негайно перериває функцію і передає конкретну помилку викликачу. throws — позначка в сигнатурі: «ця функція може завершитися помилкою». try — на боці викликача: «я знаю, що цей виклик може кинути, і беру це на себе».",
            "Разом вони роблять помилку частиною типу функції: компілятор не дасть викликати throwing-функцію без try і без обробки — або do-catch, або try?, або try!, або throws у себе в сигнатурі й проброс далі.",
            "Поруч є rethrows: функція кидає лише те, що кинув переданий їй closure. Саме так позначені map і filter — вони самі не помиляються.",
          ],
        },
        {
          q: "throw означає crash? Що буде в catch після throw LoginError.wrongPassword?",
          a: [
            "Ні, throw — це не краш, а передача значення: виконання функції припиняється і керування переходить у найближчий do-catch угору по стеку.",
            "У catch прилітає саме той екземпляр помилки. Є catch LoginError.wrongPassword — зайдеш у нього; немає — спрацює загальний catch, де змінна error містить LoginError.wrongPassword, і її можна зіставити через switch або скастити as?.",
            "Крашить лише try! на реальній помилці. І не плутай throw з fatalError чи preconditionFailure — ті навмисно валять процес, бо це баг програміста, а не очікуваний збій.",
          ],
        },
        {
          q: "try? vs try! vs do-catch — коли що?",
          a: [
            "do-catch — коли на помилку є реакція: показати Alert, повторити запит, перевести state у .failed. Це дефолт для мережі, парсингу й авторизації.",
            "try? — коли nil сам собою прийнятна відповідь і причина не потрібна: результат стає Optional. try! — обіцянка «помилки не буде»; при помилці краш, як force unwrap, тому легітимно лише для того, що гарантовано лежить у бандлі.",
            "Пастка: try? ковтає причину. У do-catch ти б побачив DecodingError з codingPath і одразу знав, яке поле не зійшлося; з try? маєш лише nil і жодної підказки, чому парсинг упав.",
          ],
        },
        {
          q: "Коли повернути User?, а коли throw?",
          a: [
            "Optional — коли відсутність результату нормальна і у викликача немає що з нею робити, крім «не знайшов»: пошук у словнику, findUser(id:) у локальному кеші. Кидати помилку на очікуваний кейс — overkill, бо змушує обгортати звичайний пошук у do-catch.",
            "throw — коли операція мала вдатися і причина збою потрібна: мережа впала, JSON не зійшовся, токен протермінований. Тут nil був би втратою інформації — ти не відрізниш 401 від відсутності інтернету, а UI на них реагує по-різному.",
            "Result<T, Error> лишається для колбеків, де throws не працює. Для async-коду достатньо async throws — Result там уже зайвий шар.",
          ],
        },
        {
          q: "Напиши enum LoginError і func login(password:) throws з двома перевірками.",
          a: [
            "Два кейси помилки — порожній пароль і невірний пароль. Функція нічого не повертає, тому throws стоїть у сигнатурі без -> Type.",
            "Перевірки через guard, від конкретнішої до загальнішої: спершу порожній рядок, потім розбіжність. У викликача — do з двома catch на конкретні кейси і загальним catch останнім; поставиш загальний першим — компілятор скаже, що решта недосяжна.",
          ],
          code: [
            "enum LoginError: Error {",
            "    case emptyPassword",
            "    case wrongPassword",
            "}",
            "",
            "func login(password: String) throws {",
            "    guard !password.isEmpty else { throw LoginError.emptyPassword }",
            "    guard password == \"1234\" else { throw LoginError.wrongPassword }",
            "    print(\"Login successful\")",
            "}",
            "",
            "do { try login(password: \"1111\") }",
            "catch LoginError.emptyPassword { print(\"Password is empty\") }",
            "catch LoginError.wrongPassword { print(\"Wrong password\") }   // ← сюди",
            "catch { print(error) }",
          ],
        },
        {
          q: "Як показати Alert користувачу після помилки мережевого запиту?",
          a: [
            "Обгортаєш try await у do-catch, у catch переводиш стан у .failed і показуєш Alert — обовʼязково з main-потоку, тому метод показу під @MainActor або через await MainActor.run.",
            "Текст не беру з error.localizedDescription для всіх випадків, а мапаю доменну помилку в людське повідомлення: .unauthorized → «Сесія завершилась, увійди знову», .server(let code) → «Спробуй пізніше», decoding → загальне «Щось пішло не так». Технічну деталь лишаю в лог.",
            "Пастка: коли ти сам скасував Task при уході з екрана, у catch прилітає CancellationError або URLError.cancelled. Alert на них показувати не треба — інакше юзер бачить помилку вже після того, як закрив екран.",
          ],
          code: [
            "@MainActor",
            "func loadOrders() async {",
            "    do {",
            "        orders = try await api.fetchOrders()",
            "    } catch is CancellationError {",
            "        return                       // сам скасував — не помилка для юзера",
            "    } catch APIError.unauthorized {",
            "        showAlert(\"Сесія завершилась\", \"Увійди знову\")",
            "    } catch {",
            "        log(error)",
            "        showAlert(\"Не вдалося завантажити\", \"Спробуй пізніше\")",
            "    }",
            "}",
          ],
        },
      ],
    },
    {
      p: "P0", name: "Асинхронність: completion handlers → async/await → Task/TaskGroup → actors",
      also: ["swift-6", "swift-20"],
      what: [
        "Асинхронність — це виконання роботи «паралельно» основному потоку, без блокування UI: мережа, диск, важкі обчислення, очікування таймерів. В iOS-співбесідах майже завжди питають еволюцію підходів: старий completion handler API → сучасний async/await → Task/TaskGroup для керування життєвим циклом → actors для безпечного спільного стану.",
        "Сучасний підхід у Swift: async/await як базовий інструмент для request/response, Task для cancellation і пріоритетів, TaskGroup для паралельних незалежних підзадач, actor для захисту mutable state. Completion handlers усе ще потрібні для старих API/SDK і деяких UIKit callback-ів — важливо вміти мостити їх у async/await.",
      ],
      sections: [
        {
          title: "1. Completion handler — класичний callback-підхід",
          text: "Completion handler — це замикання, яке викликають, коли асинхронна робота завершилась. Плюси: працює всюди, сумісно з legacy API. Мінуси: вкладеність (callback hell), складне скасування, важче читати потік помилок (Result/optional/error параметр), легко отримати retain cycle, якщо closure тримає self сильно. На співбесіді важливо сказати: «callback — це контракт: коли закінчу, викличу completion на потрібній черзі (main для UI)».",
          code: [
            "enum APIError: Error { case transport(Error), server(code: Int) }",
            "",
            "func fetchOrders(completion: @escaping (Result<[Order], APIError>) -> Void) {",
            "    URLSession.shared.dataTask(with: url) { data, response, error in",
            "        if let error { return completion(.failure(.transport(error))) }",
            "        let code = (response as? HTTPURLResponse)?.statusCode ?? -1",
            "        guard (200..<300).contains(code) else { return completion(.failure(.server(code: code))) }",
            "        // decode…",
            "        completion(.success([]))",
            "    }.resume()",
            "}",
            "",
            "fetchOrders { result in",
            "    DispatchQueue.main.async {",
            "        // оновлення UI",
            "    }",
            "}",
          ],
        },
        {
          title: "2. async/await — сучасний «прямий» потік коду",
          text: "async/await робить асинхронний код схожим на синхронний: await «припиняє» поточну задачу без блокування потоку, а виконання продовжується після завершення. Плюси: читабельно, природний error handling через throws, простіше cancellation через Task. Мінуси: не всі API ще async; інколи треба мостити callbacks; потрібно розуміти, що await — це точка можливого переключення (suspension point).",
          code: [
            "func fetchOrders() async throws -> [Order] {",
            "    let (data, response) = try await URLSession.shared.data(from: url)",
            "    let code = (response as? HTTPURLResponse)?.statusCode ?? -1",
            "    guard (200..<300).contains(code) else { throw APIError.server(code: code) }",
            "    return try JSONDecoder().decode([Order].self, from: data)",
            "}",
            "",
            "Task {",
            "    do {",
            "        let orders = try await fetchOrders()",
            "        await MainActor.run { /* update UI */ }",
            "    } catch {",
            "        await MainActor.run { /* show error */ }",
            "    }",
            "}",
          ],
        },
        {
          title: "3. Task — життєвий цикл, cancellation, пріоритети",
          text: "Task — одиниця конкурентної роботи в Swift Concurrency. Через Task ти стартуєш async роботу з UIKit (наприклад, у viewDidLoad), можеш її скасувати (cancel), задати пріоритет, і впорядкувати життєвий цикл (наприклад, скасувати запит при deinit або при новому пошуку). Важливо: cancellation кооперативне — твій код має перевіряти Task.isCancelled або викликати try Task.checkCancellation().",
          code: [
            "final class OrdersVC: UIViewController {",
            "    private var loadTask: Task<Void, Never>?",
            "",
            "    override func viewDidAppear(_ animated: Bool) {",
            "        super.viewDidAppear(animated)",
            "        loadTask = Task { [weak self] in",
            "            guard let self else { return }",
            "            do { _ = try await fetchOrders() } catch { }",
            "        }",
            "    }",
            "",
            "    override func viewDidDisappear(_ animated: Bool) {",
            "        super.viewDidDisappear(animated)",
            "        loadTask?.cancel()",
            "    }",
            "}",
          ],
        },
        {
          title: "4. TaskGroup — паралельні підзадачі (fan-out/fan-in)",
          text: "TaskGroup використовують, коли треба запустити кілька незалежних async-операцій паралельно і зібрати результати: наприклад, завантажити 10 картинок, або одночасно підтягнути меню, профіль і промо. Плюси: паралельність без ручного менеджменту потоків, структурована конкурентність (задачі живуть всередині scope). Мінуси: потрібно акуратно обробляти помилки (throwing task group) і порядок результатів (збираються в порядку завершення, не додавання).",
          code: [
            "func loadImages(urls: [URL]) async throws -> [Data] {",
            "    try await withThrowingTaskGroup(of: Data.self) { group in",
            "        for url in urls {",
            "            group.addTask {",
            "                let (data, _) = try await URLSession.shared.data(from: url)",
            "                return data",
            "            }",
            "        }",
            "        var result: [Data] = []",
            "        for try await data in group { result.append(data) }",
            "        return result",
            "    }",
            "}",
          ],
        },
        {
          title: "5. Actor — захист спільного mutable state",
          text: "Actor — це reference type, який серіалізує доступ до свого стану: одночасно виконується лише один actor-isolated фрагмент коду. Це «вбудований лок» на рівні мови, але без ручного lock/unlock. Доречно для кешів, лічильників, менеджерів стану, де є доступ з кількох задач. Мінуси: потрібне await при доступі ззовні (може зʼявитись багато suspension points), і треба розуміти, що await всередині actor-методу — «дірка», під час якої інші задачі можуть виконатись (тому інваріанти треба тримати обережно).",
          code: [
            "actor ImageCache {",
            "    private var storage: [URL: Data] = [:]",
            "    func get(_ url: URL) -> Data? { storage[url] }",
            "    func set(_ data: Data, for url: URL) { storage[url] = data }",
            "}",
            "",
            "let cache = ImageCache()",
            "Task {",
            "    let url = URL(string: \"https://example.com/a.png\")!",
            "    if let data = await cache.get(url) { print(data.count) }",
            "}",
          ],
        },
        {
          title: "6. Міст: completion → async (continuation)",
          text: "На практиці багато SDK/старих API ще повертають completion. Swift дає continuation, щоб обгорнути callback у async-функцію. Це часто питають: «як мігрувати метод з completion на async/await?». Важливо: continuation потрібно резюмити рівно один раз (інакше UB).",
          code: [
            "func fetchOrdersAsync() async throws -> [Order] {",
            "    try await withCheckedThrowingContinuation { cont in",
            "        fetchOrders { result in",
            "            cont.resume(with: result)",
            "        }",
            "    }",
            "}",
          ],
        },
      ],
      problem: "Без правильного підходу до асинхронності отримуєш: фризи UI (блокування main), callback hell, гонки даних і retain cycles. Swift Concurrency дає структуровану конкурентність: задачі мають scope (Task/TaskGroup), помилки — в типах (throws), спільний стан — захищений (actors), а cancellation — керований.",
      where: [
        "Completion handlers: старі SDK, делегатні API UIKit, колбеки в сторонніх бібліотеках.",
        "async/await: URLSession, JSONDecoder, FileManager, бізнес-операції VM/UseCase.",
        "Task: запуск async з UIKit lifecycle, cancel при уході з екрана, search cancel previous.",
        "TaskGroup: паралельно завантажити ресурси для екрана (меню+профіль+промо) або багато картинок.",
        "Actor: кеші, менеджери токена/refresh, серіалізація доступу до спільного стану між задачами.",
      ],
      traps: [
        "UI оновлюємо тільки з main: MainActor.run / @MainActor — інакше race/crash.",
        "Cancellation кооперативне: cancel() не «вбиває» роботу миттєво, якщо код не перевіряє isCancelled/checkCancellation.",
        "Task { } в VC без cancel у didDisappear/deinit → робота продовжиться після закриття екрана.",
        "TaskGroup: результати приходять у порядку завершення, не у порядку urls.",
        "Actor: await всередині actor-методу — під час нього інші задачі можуть змінити стан; тримай інваріанти.",
        "Continuation: resume рівно один раз; подвійний resume = баг.",
      ],
      qs: [
        {
          q: "Completion handler vs async/await — що змінилось концептуально і в коді?",
          a: [
            "Концептуально змінилось, хто володіє продовженням. З completion ти віддаєш замикання і втрачаєш контроль: результат прилетить «колись, десь, на невідомій черзі». З async/await продовження — це твій же наступний рядок після await, а функція просто повертає значення або кидає помилку.",
            "У коді зникає вкладеність, помилки йдуть через throws замість Result чи трійки (data, response, error), і не треба вручну стрибати через DispatchQueue.main.async — достатньо @MainActor. Cancellation стає вбудованою через Task замість власних прапорців.",
            "Ще один клас багів зникає: у completion-версії легко забути викликати completion в одній з early-return гілок — і екран назавжди залишався у стані завантаження. З throws компілятор такого шляху не дає.",
          ],
        },
        {
          q: "Як скасувати мережевий запит при уході з екрана? (Task cancel + перевірка cancellation)",
          a: [
            "Зберігаєш Task у властивості й кличеш cancel() у viewDidDisappear або deinit. Сам запит через try await URLSession реагує на скасування і кидає помилку, тож мережа обривається без твоєї участі.",
            "Але cancellation кооперативне: cancel() лише ставить прапорець. Якщо між await-ами у тебе цикл або важка обробка — перевіряй Task.isCancelled або кликай try Task.checkCancellation(), інакше робота дійде до кінця і ще й оновить UI закритого екрана.",
            "Практика для пошуку: перед стартом нового запиту скасовуй попередній Task, а в catch не показуй Alert на скасування — для користувача це не помилка.",
          ],
          code: [
            "private var loadTask: Task<Void, Never>?",
            "",
            "override func viewDidAppear(_ animated: Bool) {",
            "    super.viewDidAppear(animated)",
            "    loadTask = Task { [weak self] in",
            "        guard let self else { return }",
            "        do {",
            "            let orders = try await api.fetchOrders()",
            "            try Task.checkCancellation()   // cancel лише ставить прапорець",
            "            render(orders)",
            "        } catch { }",
            "    }",
            "}",
            "",
            "override func viewDidDisappear(_ animated: Bool) {",
            "    super.viewDidDisappear(animated)",
            "    loadTask?.cancel()",
            "}",
          ],
        },
        {
          q: "Task vs TaskGroup — коли що?",
          a: [
            "Task — одна одиниця асинхронної роботи: точка входу з синхронного коду (viewDidLoad, @IBAction) і ручка, за яку можна скасувати або задати пріоритет. TaskGroup — коли треба кілька незалежних підзадач паралельно і зібрати їхні результати.",
            "TaskGroup дає структуровану конкурентність: група не завершиться, поки не завершились усі дочірні задачі, скасування групи скасовує всіх, а помилка у throwing-групі скасовує решту. Для двох-трьох відомих наперед запитів простіше async let; TaskGroup — коли кількість динамічна, як «завантажити N картинок».",
            "Пастка: результати з групи приходять у порядку завершення, не у порядку додавання. Потрібен порядок — повертай з addTask пару (index, value) і сортуй, або збирай у словник за індексом.",
          ],
        },
        {
          q: "Actor: що він гарантує і чим відрізняється від класу + lock?",
          a: [
            "Actor — reference type, який серіалізує доступ до власного стану: у будь-який момент виконується не більше одного actor-isolated фрагмента. Data race на його властивостях неможливий за побудовою, без жодного ручного lock/unlock.",
            "Відмінність від class + lock у двох речах. Перша — ізоляцію перевіряє компілятор: до стану ззовні не дістатися без await, тому «забув взяти лок» не буває. Друга — чекання не блокує потік: задача призупиняється, а потік іде виконувати іншу роботу, тому класичного deadlock на двох взаємних локах тут не виходить.",
            "Пастка: actor захищає стан, але не атомарність послідовності. await всередині методу відкриває reentrancy — між двома await інша задача може змінити стан, тому інваріанти «через await» тримати не можна.",
          ],
        },
        {
          q: "Як обгорнути callback API у async/await через continuation?",
          a: [
            "Через withCheckedThrowingContinuation: усередині кличеш старий метод із completion, а в completion резюмуєш continuation результатом. Для API, що не помиляється, є withCheckedContinuation.",
            "Головне правило — resume рівно один раз на кожному можливому шляху. Нуль викликів означає, що задача зависне назавжди; два — краш у checked-версії (unsafe-версія дає undefined behavior). Тому continuation не годиться для колбеків, які стріляють багато разів, як делегати прогресу — там AsyncStream.",
            "Практика: якщо старий API повертає (T?, Error?), нормалізуй це в один resume — спершу перевір error, потім значення, а в else кинь власну «unknown» помилку, щоб не залишити шлях без resume.",
          ],
          code: [
            "func fetchOrdersAsync() async throws -> [Order] {",
            "    try await withCheckedThrowingContinuation { cont in",
            "        legacyAPI.fetchOrders { orders, error in",
            "            if let error {",
            "                cont.resume(throwing: error)",
            "            } else if let orders {",
            "                cont.resume(returning: orders)",
            "            } else {",
            "                cont.resume(throwing: APIError.unknown)  // жодної гілки без resume",
            "            }",
            "        }",
            "    }",
            "}",
          ],
        },
        {
          q: "Чому await — це «точка переключення» і чому це важливо для state?",
          a: [
            "await — suspension point: тут задача може призупинитися, віддати потік і продовжитись пізніше, можливо вже на іншому потоці. Це не блокування — потік не стоїть на місці, а виконує іншу роботу, тому дешево мати тисячі задач.",
            "Наслідок для стану: усе, що ти прочитав до await, після await могло змінитися. Класика — кеш-актор: перевірив «значення немає», пішов у await на мережу, а за цей час друга задача зробила той самий запит. Виходять два запити й перезапис; лікується збереженням in-flight задачі, а не повторною перевіркою.",
            "Тому два правила: перечитуй стан після await замість того, щоб покладатися на старий знімок, і не роби припущень про потік — для UI ставиш @MainActor.",
          ],
        },
      ],
    },
    {
      p: "P0", name: "let, var, static і access control",
      also: ["swift-13"],
      what: [
        "У Swift два способи оголошення змінних: let — константа (присвоїти можна лише один раз), var — змінна (можна змінювати скільки завгодно). Обидва можна оголосити без початкового значення, якщо вказати тип і гарантувати присвоєння до першого використання.",
        "static — властивість або метод належить типу, а не екземпляру (одне значення на весь тип). У class для override використовують class замість static.",
        "Access control керує видимістю: private < fileprivate < internal (дефолт) < public < open. private(set) — читати можна ззовні, писати лише всередині типу.",
      ],
      sections: [
        {
          title: "1. let — константа",
          text: "Константа зберігає значення, яке не можна змінити після першого присвоєння. Можна одразу задати значення (let name = \"Nazar\") або оголосити з типом і присвоїти пізніше — але лише один раз і до першого використання. Swift вимагає, щоб усі гілки if/switch присвоїли значення, інакше — помилка «used before being initialized».",
          code: [
            "let name = \"Nazar\"",
            "name = \"John\"              // ❌ Cannot assign to 'let'",
            "",
            "let username: String",
            "if isLoggedIn { username = \"Nazar\" } else { username = \"Guest\" }",
            "print(username)            // ✅ будь-який шлях присвоїв значення",
            "",
            "let x: String",
            "if flag { x = \"a\" }",
            "print(x)                   // ❌ якщо flag == false — не ініціалізовано",
          ],
        },
        {
          title: "2. var — змінна",
          text: "Змінну можна змінювати необмежену кількість разів. На відміну від let, повторне присвоєння дозволене. Можна оголосити без значення (var score: Int) і присвоїти пізніше.",
          code: [
            "var name = \"Nazar\"",
            "name = \"John\"",
            "name = \"Alex\"              // ✅ скільки завгодно разів",
            "",
            "var score = 0",
            "score += 10",
            "score += 5",
          ],
        },
        {
          title: "3. let vs var — порівняння",
          text: "Найкраща практика: let за замовчуванням, var лише коли значення реально змінюється. Це зменшує кількість місць, де стан може змінитись несподівано, і полегшує читання коду.",
          code: [
            "let pi = 3.14159           // не змінюється",
            "let maxPlayers = 4",
            "var score = 0              // змінюється",
            "score += 10",
          ],
        },
        {
          title: "4. static — належить типу, не екземпляру",
          text: "static let/var — одне значення на весь тип, доступ без створення екземпляра. static let — константа типу; static var — можна змінювати. У struct/enum — лише static. У class: static (не override) або class func (можна override). Типовий кейс: shared instance, factory, константи конфігурації.",
          code: [
            "struct Config {",
            "    static let apiBaseURL = \"https://api.example.com\"",
            "    static var requestCount = 0",
            "}",
            "Config.apiBaseURL            // ✅ без екземпляра",
            "",
            "final class SessionManager {",
            "    static let shared = SessionManager()",
            "    private init() {}",
            "}",
            "",
            "class Animal {",
            "    class func species() -> String { \"Animal\" }",
            "}",
            "class Dog: Animal {",
            "    override class func species() -> String { \"Dog\" }",
            "}",
          ],
        },
        {
          title: "5. Access control: private, fileprivate, internal, public, open",
          text: "Рівні доступу від найвужчого: private (лише тип + extensions у файлі) < fileprivate (увесь .swift файл) < internal (модуль/target, дефолт) < public (видно ззовні модуля) < open (як public + можна успадковувати/override ззовні). На співбесіді для звичайного iOS-аппа найчастіше питають private/fileprivate/internal; public/open — для SDK/SPM-пакетів.",
          code: [
            "class BankAccount {",
            "    private var balance: Double = 0",
            "    func deposit(_ amount: Double) { balance += amount }",
            "}",
            "// account.balance            // ❌ inaccessible",
            "",
            "fileprivate func formatPrice(_ v: Decimal) -> String { \"\\(v) ₴\" }",
            "// видно в усьому файлі, не в інших",
            "",
            "class OrderService { }         // internal за замовчуванням",
          ],
        },
        {
          title: "6. private(set) — read-only ззовні",
          text: "private(set) var — ззовні можна читати, писати лише всередині типу. Типовий патерн у ViewModel: UI читає стан, змінює лише власник. let/var визначає чи можна змінити значення; access control — хто бачить і хто може писати.",
          code: [
            "final class OrderViewModel {",
            "    private(set) var orders: [Order] = []",
            "    private let service: OrderService",
            "    init(service: OrderService) { self.service = service }",
            "    func load() async { orders = await service.fetch() }",
            "}",
            "// vm.orders = []             // ❌ setter inaccessible",
            "// print(vm.orders)           // ✅ read OK",
          ],
        },
        {
          title: "7. Приклад UIKit: let + private + static",
          text: "У UIViewController часто: let для immutable даних з init, private для subviews і деталей реалізації, static let для reuse identifier або storyboard ID, private(set) для стану, який UI читає але не мутує.",
          code: [
            "final class DetailViewController: UIViewController {",
            "    static let storyboardID = \"DetailVC\"",
            "    private let imageName: String",
            "    private(set) var isLoading = false",
            "",
            "    init(imageName: String) {",
            "        self.imageName = imageName",
            "        super.init(nibName: nil, bundle: nil)",
            "    }",
            "    required init?(coder: NSCoder) { fatalError() }",
            "",
            "    func startLoading() { isLoading = true }",
            "}",
          ],
        },
      ],
      problem: "let/var — базова дисципліна мутабельності: менше let → більше місць, де стан змінюється несподівано. static відокремлює стан типу від стану екземпляра. Access control — інкапсуляція: приховуєш деталі реалізації, залишаєш контрольований API (private(set), методи). Без цього код стає «все public» і його важко підтримувати в команді.",
      where: [
        "let за замовчуванням для моделей, констант, immutable полів у init (imageName, orderId).",
        "var — лічильники, UI-стан, буфери, що змінюються під час роботи.",
        "static let shared / factory — синглтони, конфіг, reuseID для UITableViewCell.",
        "private — subviews, сервіси, деталі VM; fileprivate — helpers у файлі.",
        "private(set) var у ViewModel/ObservableObject — View читає, змінює лише VM.",
      ],
      traps: [
        "let для class фіксує посилання, не вміст — можна змінювати властивості обʼєкта, але не переприсвоїти змінну.",
        "let без ініціалізації в усіх гілках if — compile error «used before being initialized».",
        "private у Swift мʼякший за Java: extensions того ж типу в тому ж файлі бачать private.",
        "static var — спільний mutable state між усіма; обережно з потокобезпекою.",
        "Плутати let (мутабельність значення) з private (видимість) — це різні осі.",
      ],
      qs: [
        {
          q: "Чим let відрізняється від var? Коли що використовувати за замовчуванням?",
          a: [
            "let — значення присвоюється один раз і більше не змінюється; var — переприсвоювати можна скільки завгодно.",
            "За замовчуванням let, а var — лише коли значення справді мутує. Це не стилістика: так скорочується кількість місць, де стан може змінитися, і читаючи функцію ти одразу бачиш, що в ній рухається. Компілятор ще й підказує «never mutated, consider changing to let».",
            "Пастка: let для класу фіксує посилання, а не обʼєкт — dog.name змінити можна, dog = Dog() ні. Для структури let заморожує всі поля і забороняє mutating-методи.",
          ],
        },
        {
          q: "Можна let оголосити без значення? За яких умов?",
          a: [
            "Можна: let username: String — тип обовʼязковий, бо виводити його нізвідки. Умова одна: значення присвоюється рівно раз і до першого використання, у всіх можливих гілках.",
            "Тому if isLoggedIn { username = \"Nazar\" } else { username = \"Guest\" } компілюється, а if без else — ні: існує шлях, де змінна не ініціалізована, і компілятор скаже «used before being initialized».",
            "Так зручно писати справжній let замість var із фейковим початковим значенням. Для stored property класу принцип той самий: let без значення мусить бути присвоєний у кожному init — і до super.init.",
          ],
          code: [
            "let username: String",
            "if isLoggedIn { username = \"Nazar\" } else { username = \"Guest\" }",
            "print(username)        // ✅ значення присвоїв кожен шлях",
            "",
            "let x: String",
            "if flag { x = \"a\" }",
            "print(x)               // ❌ used before being initialized",
          ],
        },
        {
          q: "private vs fileprivate vs internal — різниця і приклад?",
          a: [
            "private — видно у самому типі і в його extensions у тому ж файлі. fileprivate — у всьому .swift файлі, включно з іншими типами в ньому. internal — у всьому модулі або таргеті, і це дефолт, який писати не треба.",
            "На практиці: private для subviews і сервісів усередині VC, fileprivate для helper-функції, якою користуються два типи в одному файлі, internal для більшості коду застосунку. public і open — вже історія про SDK і SPM-пакети.",
            "Деталь, яку люблять питати: private у Swift мʼякший, ніж у Java — extension того самого типу в тому самому файлі бачить private-члени. Тому private не означає «недоступно нікому, крім тіла типу».",
          ],
          code: [
            "final class OrderService {           // internal за замовчуванням",
            "    private var cache: [Order] = []  // лише тип + його extensions у файлі",
            "}",
            "",
            "extension OrderService {",
            "    func count() -> Int { cache.count }   // бачить private — той самий файл",
            "}",
            "",
            "fileprivate func formatPrice(_ v: Decimal) -> String { \"\\(v) ₴\" }",
          ],
        },
        {
          q: "Що таке private(set) і навіщо в ViewModel?",
          a: [
            "private(set) var — getter лишається на звичайному рівні (internal за дефолтом), а setter стає private: читати можуть усі, писати лише сам тип.",
            "У ViewModel це і є контракт з UI: View читає orders чи state, але змінити їх може лише load() усередині VM. Без цього будь-який екран міг би присвоїти vm.orders = [] — і потім джерело зміни стану неможливо знайти, бо писати могли всі.",
            "Деталь: працює лише для var, бо let і так незмінний. Той самий трюк є як fileprivate(set), а для бібліотек — public private(set): ззовні модуля читають, пише лише сам тип.",
          ],
        },
        {
          q: "static let vs static var — коли що? Чим static відрізняється від звичайної властивості?",
          a: [
            "static означає, що властивість належить типу, а не екземпляру: одне значення на весь тип, доступ через Config.apiBaseURL без створення обʼєкта. Звичайна властивість живе окремо в кожному екземплярі.",
            "static let — константи й синглтони: базовий URL, reuseIdentifier комірки, static let shared. Він ще й лінивий: ініціалізується один раз при першому доступі, і Swift гарантує, що це станеться безпечно навіть з кількох потоків.",
            "static var — спільний мутабельний стан на весь процес. Без синхронізації це готовий data race, а в тестах він тече між кейсами і дає плаваючі падіння. Потрібен таки — сховай за actor або хоча б за private(set) з методом-мутатором.",
          ],
          code: [
            "struct Config {",
            "    static let apiBaseURL = \"https://api.example.com\"",
            "}",
            "",
            "final class SessionManager {",
            "    static let shared = SessionManager()   // lazy + один раз",
            "    private init() {}",
            "}",
            "",
            "class Animal { class func species() -> String { \"Animal\" } }",
            "class Dog: Animal { override class func species() -> String { \"Dog\" } }",
          ],
        },
        {
          q: "let imageName в init UIViewController — чому let, а не var?",
          a: [
            "Бо це вхідний параметр екрана, який після створення не змінюється: екран деталей відкривають для конкретного зображення. let робить це явним контрактом — ніхто не переприсвоїть imageName посеред життєвого циклу, і в жодному місці не треба питати «а чи воно ще те саме?».",
            "Технічно let-властивість присвоюється в init до super.init, і компілятор гарантує, що вона вже є, коли екран існує. З var довелося б або давати заглушку, або робити Optional — і потім розгортати її у viewDidLoad та в кожному методі.",
            "Разом із private це стандарт для UIKit-екранів: private let imageName: String. Нові дані — це новий екран або окремий метод configure, а не мутація поля з боку.",
          ],
          code: [
            "final class DetailViewController: UIViewController {",
            "    static let storyboardID = \"DetailVC\"",
            "    private let imageName: String",
            "    private(set) var isLoading = false",
            "",
            "    init(imageName: String) {",
            "        self.imageName = imageName",
            "        super.init(nibName: nil, bundle: nil)",
            "    }",
            "    required init?(coder: NSCoder) { fatalError(\"init(coder:) not used\") }",
            "}",
          ],
        },
      ],
    },
    {
      p: "P1", name: "Swift 6: strict concurrency, ізоляція і міграція",
      also: ["swift-6", "swift-18"],
      what: [
        "Swift 6 language mode — це не новий синтаксис, а нові правила перевірки. Компілятор знає ізоляцію кожної декларації: @MainActor, конкретний actor або nonisolated — і забороняє перетинати ці границі з non-Sendable значенням. Те, що у Swift 5 було попередженням або лишалось непоміченим, стає помилкою збірки. Рантайм при цьому не змінюється: змінюється те, що взагалі компілюється.",
        "Одиниця ізоляції — actor: усередині нього в кожен момент виконується один виклик. Global actor дозволяє привʼязати до одного домену багато різних типів, і @MainActor — його головний випадок. Ставиться на тип (усі члени ізольовані), на окремий метод або на окрему властивість. UIView і UIViewController у SDK вже позначені @MainActor.",
        "Sendable — обіцянка «це значення можна безпечно віддати в інший домен ізоляції». Struct та enum, усі поля яких Sendable, отримують конформанс автоматично (для public-типів його оголошують явно). Class — майже ніколи: він передається посиланням, і будь-яка mutable-властивість стає спільним станом. Виняток — final class лише з let-полями Sendable-типів.",
        "Перетин границі перевіряється не тільки за типом, а й за тим, чи не лишилось на значення інших посилань. Тому non-Sendable обʼєкт можна легально передати як sending-параметр — «віддаю назовсім і сам більше не торкаюсь». Closure успадковує ізоляцію місця, де створений; @Sendable-closure — ні, і всі його захоплення мусять бути Sendable.",
      ],
      problem: "Data race — найдорожчий клас багів: падіння без стабільного репро, зіпсовані колекції, «іноді» неправильний UI. TSan ловить його лише тоді, коли гонка реально сталася під час запуску. Модель ізоляції переносить перевірку у compile time: збірка не пройде, якщо non-Sendable значення перетинає границю ізоляції. Плата — міграція, під час якої компілятор показує всі місця, що працювали випадково.",
      where: [
        "Моделі й DTO Eatery Club: struct-и меню, позиції кошика, замовлення стають Sendable без жодного рядка коду — саме з цього шару починають міграцію.",
        "Сервіси: кеш меню, токен-провайдер, шар оплати — або actor, або тип із незмінним станом; токен-провайдер із дедуплікацією refresh природно стає actor-ом.",
        "UIKit-шар природно @MainActor: VC, view, ViewModel уже там, тому міграція цього шару здебільшого означає прибрати ручні DispatchQueue.main.async, а не переписати екрани.",
        "Server-driven секції home page: описи секцій парсяться у фоні і йдуть у @MainActor-рендер — саме тут Sendable на моделях перестає бути формальністю.",
        "Legacy GCD-острови (старі SDK, обробка пушів, оновлення Live Activities) обгортаються continuation-ом або actor-фасадом, а не переписуються одночасно з усім іншим.",
        "Поетапне включення: рівень перевірки конкурентності і версія мови задаються у налаштуваннях таргету, тому модуль моделей можна тримати у строгому режимі, поки основний застосунок ще ні.",
      ],
      code: [
        "struct MenuItem: Sendable { let id: String; let title: String }  // value-поля → Sendable",
        "",
        "actor MenuCache {                          // власний домен ізоляції",
        "    private var items: [MenuItem] = []",
        "    func store(_ new: [MenuItem]) { items = new }",
        "    nonisolated let source = \"api\"          // let без стану — доступний звідусіль",
        "}",
        "",
        "@MainActor final class MenuVM {            // весь тип ізольований на main",
        "    private let cache = MenuCache()",
        "    private(set) var items: [MenuItem] = []",
        "    func load() async throws {",
        "        let fetched = try await api.menu()  // await відпускає main і повертається на main",
        "        await cache.store(fetched)          // MenuItem: Sendable → границя легальна",
        "        items = fetched                    // ми знову на main, DispatchQueue не потрібен",
        "    }",
        "    nonisolated func analyticsKey() -> String { \"menu\" }  // стану не торкається",
        "}",
      ],
      traps: [
        "«Просто @MainActor всюди» компілюється — і застосунок починає підлагувати: парсинг великого JSON і робота з диском виїжджають на головний потік. Симптом — просідання скролу там, де раніше все було гладко.",
        "«Capture of non-Sendable type in a @Sendable closure» означає не «додай @unchecked», а «ти віддаєш посилання на mutable-обʼєкт у інший домен». Після @unchecked симптом той самий рандомний креш, тільки вже без попередження компілятора.",
        "«Main actor-isolated property can not be referenced from a nonisolated context» найчастіше вилітає у deinit і в делегатних методах старих SDK: deinit не ізольований, тому прибирати з нього @MainActor-стан не можна.",
        "@MainActor на класі протікає далі: успадковується підкласами і може прийти через конформанс протоколу. Симптом — сервісний тип, який «раптом» став main-actor, і вся його робота переїхала на головний потік.",
        "static let shared у не-Sendable класі дає помилку про concurrency-unsafe глобальний стан. nonisolated(unsafe) її глушить, але синглтон від цього безпечним не стає — це позначка «розберусь пізніше», а не рішення.",
        "@preconcurrency import прибирає попередження від старого модуля, але не змінює його поведінку: якщо SDK кличе колбек не на main, компілятор промовчить, а креш на оновленні UI лишиться.",
      ],
      qs: [
        {
          q: "Що конкретно змінює Swift 6 language mode проти Swift 5 зі строгими перевірками — це зміна компілятора чи рантайму?",
          a: [
            "Виключно компілятора. Правила ті самі, змінюється суворість: у Swift 6 порушення ізоляції — помилка збірки, у Swift 5 — попередження або тиша. Код, який зібрався в обох режимах, у рантаймі поводиться однаково.",
            "Механіка така: кожна декларація має відому ізоляцію — global actor, конкретний actor або nonisolated. Перетин границі дозволений лише Sendable-значенням чи явною передачею володіння. Це статичний аналіз, жодних перевірок під час виконання він не додає.",
            "Практичний висновок: увімкнення строгого режиму не псує код, а показує місця, які були неправильні й працювали випадково. Тому міграція — це переважно перечитування власних інваріантів, а не переписування логіки.",
          ],
        },
        {
          q: "Що потрібно від типу, щоб він був Sendable? Чому struct з value-полями конформує сам, а class майже ніколи?",
          a: [
            "Sendable означає: значення цього типу можна віддати в інший домен ізоляції без гонки. Це виконується, якщо тип або незмінний, або має копіювальну семантику, або сам серіалізує доступ до свого стану.",
            "Struct і enum, усі поля яких Sendable, конформують автоматично: при передачі вони копіюються, спільного стану просто не виникає. Для public-типів конформанс оголошують явно, щоб він став частиною контракту модуля.",
            "Class передається посиланням: два домени тримають один обʼєкт, і будь-яка var — потенційна гонка. Конформувати може final class лише з let-полями Sendable-типів. Actor і @MainActor-типи Sendable за побудовою — доступ до їхнього стану вже серіалізований.",
          ],
        },
        {
          q: "Коли @unchecked Sendable виправданий, а коли це просто заглушення помилки?",
          a: [
            "Виправданий, коли синхронізація реальна, але компілятор її не бачить: клас із власним NSLock або серійною чергою, який проводить через них усі звертання до стану. Тоді @unchecked Sendable документує гарантію, що вже існує.",
            "Другий законний випадок — обгортка над типом із чужого світу: ObjC-клас або сторонній SDK, про потокобезпечність якого ти знаєш із документації, а вихідники змінити не можеш.",
            "Незаконний випадок — коли його ставлять, щоб зникла помилка збірки. Симптом приходить у релізі: рандомний креш, зіпсована колекція, зникле значення. І жодного попередження, бо підпис «я гарантую» ти вже поставив.",
          ],
        },
        {
          q: "У чому різниця між @MainActor на типі, на методі і на властивості? Що робить nonisolated?",
          a: [
            "На типі — усі його члени ізольовані на main; це вибір для VC, view і ViewModel. На методі — тільки цей метод, решта типу лишається неізольованою: зручно для одного колбека, який торкається UI.",
            "На властивості — ізольована лише вона: тип живе у фоні, а конкретне поле читається й пишеться тільки з main. nonisolated — обернена операція: знімає ізоляцію з члена ізольованого типу, щоб його можна було викликати звідусіль.",
            "Умова для nonisolated жорстка: такий член не має права торкатися ізольованого mutable-стану. Тому туди йдуть чисті функції, id, computed property над let — типово те, що потрібне для Equatable, Hashable або опису в логах.",
          ],
          code: [
            "@MainActor final class CartVC: UIViewController { /* весь тип на main */ }",
            "",
            "final class OrderService {                        // сам тип не ізольований",
            "    @MainActor private var lastShownID: String?   // ізольована одна властивість",
            "    @MainActor func present(_ order: Order) { }    // ізольований один метод",
            "    func upload(_ order: Order) async throws { }   // решта — поза main",
            "}",
            "",
            "actor PriceCache {",
            "    private var values: [String: Decimal] = [:]",
            "    nonisolated let currency = \"UAH\"              // не залежить від стану",
            "    nonisolated func key(for id: String) -> String { \"price-\\(id)\" }",
            "}",
          ],
        },
        {
          q: "У старому GCD-коді компілятор кинув «capture of non-Sendable type in a @Sendable closure». Що це насправді означає і як ти це чиниш?",
          a: [
            "Означає буквально: closure піде виконуватись у іншому домені ізоляції, а ти захоплюєш у нього посилання на обʼєкт, який небезпечно передавати. Після передачі до одного стану зможуть дотягнутись двоє одночасно.",
            "Найчастіше винне захоплене self класу-сервіса або mutable-модель. Правильний ремонт — передавати значення, а не обʼєкт: витягнути потрібні Sendable-поля до входу в closure, або зробити сервіс actor-ом і звертатись до нього через await.",
            "Якщо це колбек старого SDK, який документовано приходить на головний потік, є акуратний варіант: MainActor.assumeIsolated. Але це саме твердження, а не перевірка — якщо колбек прийшов не на main, буде креш замість тихої гонки.",
          ],
          code: [
            "// ❌ у @Sendable closure заходить посилання на non-Sendable обʼєкт",
            "queue.async { self.cache.items = fetched }",
            "",
            "// ✅ 1: передаємо значення, а не обʼєкт",
            "let snapshot = fetched                     // [MenuItem] — Sendable",
            "Task { await cache.store(snapshot) }",
            "",
            "// ✅ 2: колбек старого SDK, який документовано приходить на main",
            "func sdkDidFinish() {",
            "    MainActor.assumeIsolated { self.render() }   // твердження, не перевірка",
            "}",
          ],
        },
        {
          q: "Як би ти мігрував реальний проєкт на strict concurrency і чому не можна просто ввімкнути все одразу?",
          a: [
            "Починав би не з прапорців, а з порядку шарів. Спершу моделі: struct-и й enum-и стають Sendable майже безкоштовно. Далі сервіси — мережа, кеш, токени: вони або actor, або тип із незмінним станом.",
            "Потім UIKit-шар. Він природно @MainActor, і для нього міграція здебільшого означає прибрати ручні DispatchQueue.main.async. Перевірка вмикається поетапно — по таргету або по модулю в налаштуваннях збірки, тому нижні шари можна тримати у строгому режимі раніше за застосунок.",
            "Чому не все одразу: у великому проєкті строгий режим дає сотні помилок, де реальні гонки перемішані з формальностями від старих модулів. Таку пачку неможливо ні зробити, ні відревʼювати одним PR. Модуль за модулем кожна помилка ще має контекст.",
          ],
        },
      ],
    },
  ],
};
